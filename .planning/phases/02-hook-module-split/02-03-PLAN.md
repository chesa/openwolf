---
phase: 02-hook-module-split
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/02-hook-module-split/02-VERIFICATION.md
autonomous: true
requirements:
  - HOOK-01
  - HOOK-02
  - COMPAT-01
  - COMPAT-02

must_haves:
  truths:
    - "tsc --noEmit -p tsconfig.hooks.json exits 0 (covers the 6 hook consumers; HOOK-02 and COMPAT-02)"
    - "tsc --noEmit (main tsconfig) exits 0 (covers src/scanner/anatomy-scanner.ts which imports parseAnatomy from ../hooks/shared.js; HOOK-02 for the 7th consumer)"
    - "pnpm build:hooks emits dist/hooks/shared.js (and the six wolf-*.js files) without errors"
    - "node dist/bin/openwolf.js update copies dist/hooks/*.js to .wolf/hooks/*.js, including the new wolf-*.js siblings"
    - "Running `node -e \"import('./.wolf/hooks/shared.js').then(m => console.log(Object.keys(m).length))\"` prints 19 (18 values + WorktreeContext type, though type-only exports are erased at runtime so the count may be 18)"
    - "pnpm test passes (vitest run; the existing shared.test.ts suite still passes through the barrel)"
    - "Each wolf-* module's token count is ≤ 4,000 (HOOK-01)"
  artifacts:
    - path: ".planning/phases/02-hook-module-split/02-VERIFICATION.md"
      provides: "Phase 2 verification record listing each gate and its result"
      min_lines: 40
  key_links:
    - from: "src/hooks/shared.ts (barrel)"
      to: "src/hooks/wolf-*.ts"
      via: "re-exports resolve at compile time (tsc) and at runtime (Node16 module resolution)"
      pattern: "tsc --noEmit -p tsconfig.hooks.json.*exit 0"
    - from: "src/scanner/anatomy-scanner.ts"
      to: "src/hooks/shared.ts (barrel)"
      via: "import { parseAnatomy, type AnatomyEntry } from \"../hooks/shared.js\""
      pattern: "tsc --noEmit.*exit 0"
---

<objective>
Run the four required verification gates from D-08 and the bonus checks
(vitest, full build, runtime smoke). Confirm the split satisfies HOOK-01
(token budget), HOOK-02 (consumer backward compat), COMPAT-01 (named
exports), and COMPAT-02 (no circular imports). Write a verification record
to 02-VERIFICATION.md. Update ROADMAP.md and STATE.md to mark phase 2 done.

Purpose: Plans 02-01 and 02-02 create the modules and the barrel, but
neither runs a full end-to-end check. This plan is the final gate that
proves the refactor works for every consumer — the 6 hook files AND
the 7th consumer (anatomy-scanner.ts) AND the test suite.

Output: 02-VERIFICATION.md with the four required gates plus bonus checks
recorded. ROADMAP.md and STATE.md updated.

DOES NOT modify any source file. This plan is verification only. If a
gate fails, the executor must surface the failure and stop — do NOT
attempt fixes; that is the orchestrator's call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-hook-module-split/02-CONTEXT.md
@.planning/phases/02-hook-module-split/02-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

# Source files (read for sanity-checking what should compile)
@src/hooks/shared.ts (the new barrel)
@src/hooks/wolf-paths.ts
@src/hooks/wolf-files.ts
@src/hooks/wolf-json.ts
@src/hooks/wolf-anatomy.ts
@src/hooks/wolf-describe.ts
@src/hooks/wolf-misc.ts
@src/hooks/worktree-helper.ts
@src/hooks/shared.test.ts
@src/scanner/anatomy-scanner.ts (the 7th consumer)
@tsconfig.hooks.json
@tsconfig.json
@package.json
@.planning/config.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run the four required verification gates from D-08 and token-budget check from HOOK-01</name>
  <files>
    .planning/phases/02-hook-module-split/02-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/02-hook-module-split/02-CONTEXT.md §"D-08: COMPAT-02 verification" (the four required gates)
    - .planning/phases/02-hook-module-split/02-RESEARCH.md §"Common Pitfalls" Pitfall 5 (the runtime smoke check recipe)
    - .planning/REQUIREMENTS.md §"Backward Compatibility" COMPAT-01 and COMPAT-02
    - src/hooks/shared.ts (the new barrel — confirm export count before running tsc)
    - package.json §"scripts" (pnpm build:hooks, pnpm test, pnpm build)
    - tsconfig.hooks.json (verifies the include path and module mode)
  </read_first>
  <action>
    Run each of the four required D-08 gates and the HOOK-01 token-budget
    check. Record the output of each in
    .planning/phases/02-hook-module-split/02-VERIFICATION.md using the
    Write tool (this is a NEW file — does not yet exist).

    **Gate 1: `tsc --noEmit -p tsconfig.hooks.json`** (COMPAT-02)
    - Run: `pnpm exec tsc --noEmit -p tsconfig.hooks.json` (or `npx tsc --noEmit -p tsconfig.hooks.json`)
    - This compiles all of src/hooks/ WITHOUT emitting, including the new wolf-*.ts files AND the new barrel AND all 6 hook consumer files AND shared.test.ts.
    - Expected: exit code 0; no output. If errors appear, they MUST be diagnosed by reading the message — typical issues are (a) a missing `.js` extension in a re-export, (b) a circular import detected by Node16, (c) a typo in a name.
    - The 6 hook consumers, the test file, and the barrel all need to type-check. If tsc passes, COMPAT-02 is satisfied.

    **Gate 2: `tsc --noEmit`** (main tsconfig — covers the 7th consumer)
    - Run: `pnpm exec tsc --noEmit` (no -p flag; uses the root tsconfig.json)
    - This compiles src/scanner/anatomy-scanner.ts which imports `parseAnatomy` and `AnatomyEntry` from `../hooks/shared.js`. The barrel must re-export both.
    - Expected: exit code 0; no output. If errors appear, the most likely cause is a missing re-export of `parseAnatomy` or `AnatomyEntry` in the barrel.
    - This is the gate that catches the "forgot the scanner consumer" pitfall from RESEARCH.md Pitfall 1.

    **Gate 3: `pnpm build:hooks`** (D-08 required)
    - Run: `pnpm build:hooks`
    - This compiles src/hooks/**/*.ts to dist/hooks/*.js (per tsconfig.hooks.json's outDir).
    - Expected: exit code 0. dist/hooks/shared.js, dist/hooks/wolf-paths.js, dist/hooks/wolf-files.js, dist/hooks/wolf-json.js, dist/hooks/wolf-anatomy.js, dist/hooks/wolf-describe.js, dist/hooks/wolf-misc.js, dist/hooks/worktree-helper.js all exist.
    - Verify by listing: `ls dist/hooks/`. All 8 files should be present.

    **Gate 4: `node dist/bin/openwolf.js update`** (D-08 required)
    - Prerequisite: Gate 3 must have succeeded (dist/ must be populated).
    - Run: `pnpm exec node dist/bin/openwolf.js update`
    - This copies dist/hooks/*.js to .wolf/hooks/*.js.
    - Expected: exit code 0. The eight files from Gate 3 are now in .wolf/hooks/.
    - Verify by listing: `ls .wolf/hooks/`. All 8 files should be present, including the new wolf-* siblings.

    **Gate 5: Runtime smoke check** (D-08 optional bonus, RECOMMENDED per RESEARCH.md Pitfall 5)
    - Run from the project root:
      `node -e "import('./.wolf/hooks/shared.js').then(m => console.log('exports:', Object.keys(m).sort().join(', ')))"`
    - Expected output: a comma-separated list of all 18 named value exports
      (note: the WorktreeContext TYPE re-export is erased at runtime since
      types don't exist at runtime, so 18 names is expected, not 19).
    - The expected names (sorted): `AnatomyEntry` (only if re-exported as value — usually erased),
      `appendMarkdown`, `ensureSessionDir`, `ensureWolfDir`, `estimateTokens`,
      `extractDescription`, `getSessionDir`, `getWolfDir`, `getWorktreeContext`,
      `isWolfFile`, `normalizePath`, `parseAnatomy`, `readJSON`, `readMarkdown`,
      `readStdin`, `serializeAnatomy`, `timeShort`, `timestamp`, `writeJSON`.
      Note: `AnatomyEntry` is a TypeScript interface, so it is erased at
      runtime. Expect 18 names.
    - This catches the rare case where the barrel compiles but a re-export
      resolution fails at runtime (a barrel pattern runtime gotcha).

    **Gate 6: `pnpm test`** (D-08 optional bonus)
    - Run: `pnpm test`
    - This runs the vitest suite, including `src/hooks/shared.test.ts` which
      exercises the public surface of shared.ts (getWolfDir, getSessionDir,
      ensureSessionDir, getWorktreeContext, writeJSON).
    - Expected: all existing tests pass. The mock at shared.test.ts:13
      (`vi.mock("./worktree-helper.js", ...)`) must continue to work —
      wolf-paths.ts (the new home of the functions that consume
      `detectWorktreeContextRaw`) must import from the same path
      `./worktree-helper.js`.
    - The test at shared.test.ts:181-208 (cache hit) verifies that
      `_cachedWorktreeCtx` is set after the first call, which is
      internal to wolf-paths.ts.

    **Gate 7: HOOK-01 token-budget check**
    - For each wolf-* module, run: `wc -c src/hooks/wolf-<name>.ts`
    - The byte counts are 5,578-6,375 for wolf-describe.ts PRE-shrinkage.
      Post-shrinkage target: ~11,500 chars (~3,000 tokens).
    - Per RESEARCH.md §"Token Budget Resolution", use the 3.5 chars/token
      conservative ratio:
      - wolf-paths.ts: ~2,605 chars / 3.5 = ~745 tokens (target ≤ 4,000)
      - wolf-files.ts: ~4,571 chars / 3.5 = ~1,306 tokens
      - wolf-json.ts: ~2,970 chars / 3.5 = ~849 tokens
      - wolf-anatomy.ts: ~1,673 chars / 3.5 = ~478 tokens
      - wolf-describe.ts: ~11,500 chars / 3.5 = ~3,290 tokens
      - wolf-misc.ts: ~1,057 chars / 3.5 = ~302 tokens
    - Compute the actual char count of each new file and divide by 3.5.
    - If ANY module exceeds 4,000 tokens, the gate fails and the executor
      must surface the issue. (This is the HOOK-01 failure mode — the
      shrinkage in 02-01 must have brought wolf-describe.ts down to
      ~3,000 tokens.)

    **Writing the verification record:**
    Write 02-VERIFICATION.md with the following structure:

    ```
    # Phase 2: Hook Module Split - Verification

    **Verified:** <ISO date>
    **Phase goal:** Split shared.ts into focused concern modules while maintaining backward compatibility

    ## Gate Results

    | # | Gate | Command | Result | Notes |
    |---|------|---------|--------|-------|
    | 1 | tsc hooks (COMPAT-02) | `tsc --noEmit -p tsconfig.hooks.json` | PASS/FAIL | <exit code, output summary> |
    | 2 | tsc main (7th consumer) | `tsc --noEmit` | PASS/FAIL | <exit code, output summary> |
    | 3 | Build hooks | `pnpm build:hooks` | PASS/FAIL | <dist/hooks/ contents> |
    | 4 | Update .wolf/hooks | `node dist/bin/openwolf.js update` | PASS/FAIL | <.wolf/hooks/ contents> |
    | 5 | Runtime smoke | `node -e "import('./.wolf/hooks/shared.js')..."` | PASS/FAIL | <export count and names> |
    | 6 | vitest | `pnpm test` | PASS/FAIL | <test count, pass/fail> |
    | 7 | Token budget (HOOK-01) | `wc -c` per module | PASS/FAIL | <per-module char/token counts> |

    ## Requirement Coverage

    | ID | Status | Evidence |
    |----|--------|----------|
    | HOOK-01 | PASS/FAIL | <token counts> |
    | HOOK-02 | PASS/FAIL | <Gates 1, 2, 3, 4, 5 — all consumers import through barrel> |
    | COMPAT-01 | PASS/FAIL | <Gate 5 — all 18 names importable> |
    | COMPAT-02 | PASS/FAIL | <Gate 1 — tsc hooks passes with no circular imports> |

    ## Summary

    <one-paragraph summary: did the refactor succeed? If any gate failed,
    list the failure here and stop — do NOT attempt fixes.>
    ```

    **Critical rules:**
    - Run every gate. Do not skip Gate 5 (the runtime smoke check) even
      though it is "optional" — RESEARCH.md Pitfall 5 strongly recommends
      it and it costs 5 seconds.
    - Capture the EXACT command output for each gate (or a representative
      summary) in the verification record.
    - If a gate FAILS, do not attempt to fix it. Write the failure to
      02-VERIFICATION.md and stop. The orchestrator will surface the
      failure to the user.
    - This plan is VERIFICATION ONLY. Do not modify any source file. If
      a gate fails, the only file you may create/modify is
      02-VERIFICATION.md (to record the failure).
  </action>
  <acceptance_criteria>
    - `.planning/phases/02-hook-module-split/02-VERIFICATION.md` exists.
    - The file contains a "Gate Results" table with all 7 gates and a PASS/FAIL verdict for each.
    - The file contains a "Requirement Coverage" table with all 4 requirement IDs (HOOK-01, HOOK-02, COMPAT-01, COMPAT-02) and PASS/FAIL for each.
    - All four required D-08 gates (tsc hooks, tsc main, pnpm build:hooks, openwolf update) have a verdict.
    - Gate 5 (runtime smoke check) has a recorded export count and the comma-separated list of names.
    - Gate 7 (HOOK-01 token budget) has a per-module char or token count for all 6 wolf-* modules, with a verdict.
    - If any gate FAILED, the file contains a "## Failure Summary" section listing which gates failed and their exact error output.
    - If all gates PASSED, the file contains a "## Summary" section stating the refactor is verified and the success criteria are met.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/02-hook-module-split/02-VERIFICATION.md && grep -c '^| ' .planning/phases/02-hook-module-split/02-VERIFICATION.md && grep -E 'PASS|FAIL' .planning/phases/02-hook-module-split/02-VERIFICATION.md | wc -l]<]minimax[>[</automated>
  </verify>
  <done>
    All 7 verification gates have been run. The results are recorded in
    02-VERIFICATION.md. If all 7 gates pass, HOOK-01/HOOK-02/COMPAT-01/COMPAT-02
    are all satisfied and the refactor is verified. If any gate fails, the
    failure is recorded and surfaced; no source file modifications are made
    by this plan.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update ROADMAP.md and STATE.md to mark phase 2 complete</name>
  <files>
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/ROADMAP.md (current state — find the Phase 2 section)
    - .planning/STATE.md (current state — find the Progress and "Current Position" sections)
    - .planning/phases/02-hook-module-split/02-VERIFICATION.md (created in Task 1 — must show all gates PASS before updating state)
  </read_first>
  <action>
    **ONLY DO THIS TASK IF TASK 1 REPORTED ALL GATES PASSING.** If any gate
    failed, skip this task and let the orchestrator surface the failure.

    Update two files to reflect the completed phase:

    **File 1: .planning/ROADMAP.md**
    - Find the Phase 2 line (~line 10): `- [ ] **Phase 2: Hook Module Split**`
    - Change the checkbox to `- [x] **Phase 2: Hook Module Split**`
    - Add a completed date after the description: `- [x] **Phase 2: Hook Module Split** - Split shared.ts into focused concern modules with re-export facade; verify backward compatibility (completed YYYY-MM-DD)`
    - Find the Phase 2 plan list (lines ~45-47):
      ```
      Plans:
      - [ ] 02-01: Analyze shared.ts and identify natural split boundaries...
      - [ ] 02-02: Extract modules and create re-export facade for backward compatibility
      - [ ] 02-03: Verify TypeScript compilation and all named exports remain importable
      ```
    - Change each plan's checkbox to `[x]`:
      ```
      Plans:
      - [x] 02-01: ...
      - [x] 02-02: ...
      - [x] 02-03: ...
      ```
    - Find the Progress table (near the bottom). Update the Phase 2 row:
      - `Plans Complete` column: `0/3` → `3/3`
      - `Status` column: `Not started` → `Complete`
      - `Completed` column: `-` → today's date (YYYY-MM-DD)

    **File 2: .planning/STATE.md**
    - Find the `## Current Position` section. Update:
      - `Plan:` line: `Not started` → `Complete`
      - `Status:` line: `Ready to plan` → `Complete`
      - `Last activity:` line: `2026-06-02` → today's date (YYYY-MM-DD)
      - `Progress:` line: `[░░░░░░░░░░] 0%` → `[██████░░░░] 50%` (phase 2 of 4 complete)
    - Find the `### Decisions` subsection. Add a new bullet:
      - `- Phase 2: shared.ts split shipped — 6 wolf-* modules + barrel re-export, all 7 consumers preserved`
    - Find the `### Pending Todos` subsection. Leave it empty (no new todos).
    - Find the `### Blockers/Concerns` subsection. Leave it empty.

    **Critical rules:**
    - Use the Edit tool (not Write) for these two files — they have content
      that must be preserved, and Edit lets you make targeted changes.
    - Make exactly the changes described above. Do not add new sections
      or prose.
    - Today's date is 2026-06-02 per the system context. Use that as
      the completion date.
    - The progress bar in STATE.md uses block characters. Phase 2 of 4 =
      50% = 5 filled blocks out of 10: `[█████░░░░░] 50%`.
  </action>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` line 10 now starts with `- [x] **Phase 2: Hook Module Split**` (not `- [ ]`).
    - `.planning/ROADMAP.md` Phase 2 plan list has all three plan checkboxes as `[x]`.
    - `.planning/ROADMAP.md` Progress table Phase 2 row shows `3/3`, `Complete`, and `2026-06-02`.
    - `.planning/STATE.md` `## Current Position` block has `Plan: Complete`, `Status: Complete`, and a `Progress:` line reading `[█████░░░░░] 50%`.
    - `.planning/STATE.md` `### Decisions` block has a Phase 2 completion entry.
    - Both files are otherwise unchanged (no other sections touched, no
      prose added beyond what the action describes).
  </acceptance_criteria>
  <verify>
    <automated>grep -E 'Phase 2: Hook Module Split' .planning/ROADMAP.md | head -1 && grep -E '02-0[123]:' .planning/ROADMAP.md && grep -E 'Plan:|Status:|Progress:' .planning/STATE.md | head -3]<]minimax[>[</automated>
  </verify>
  <done>
    ROADMAP.md and STATE.md both reflect phase 2 as complete. The 3-phase
    Sprint progress bar shows 50%. The decisions log has a Phase 2 entry.
    No source files were modified (only the planning artifacts).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan runs verification only; no code or trust boundaries added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-Verify-01 | Information Disclosure | Verification record | mitigate | 02-VERIFICATION.md records the exact output of each gate. If a gate fails, the failure is fully documented for the orchestrator to act on. |
| T-02-Verify-02 | Denial of Service | Gate failure | mitigate | Each gate runs independently. If Gate 3 fails (build:hooks), Gate 4 (openwolf update) is not run because dist/ won't be populated. The verification record notes this skip. |
| T-02-Verify-03 | Repudiation | ROADMAP/STATE update without verification | mitigate | Task 2 explicitly guards the ROADMAP/STATE update behind a "Task 1 reported all gates passing" check. If any gate failed, Task 2 is skipped, and the phase is NOT marked complete in the planning artifacts. |
| T-02-Verify-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. |

## Trust Boundary Notes

This plan is a verification gate. It records evidence that the refactor
succeeded (or failed). It does not modify the codebase — only the
planning artifacts (02-VERIFICATION.md, ROADMAP.md, STATE.md).
</threat_model>

<verification>
- 02-VERIFICATION.md exists with all 7 gates recorded
- All 4 D-08 required gates have a verdict
- All 4 requirement IDs (HOOK-01, HOOK-02, COMPAT-01, COMPAT-02) have a coverage status
- If all gates passed, ROADMAP.md and STATE.md are updated to mark phase 2 complete
</verification>

<success_criteria>
- [ ] 02-VERIFICATION.md exists with all 7 gate results
- [ ] All 4 required D-08 gates passed
- [ ] HOOK-01 token budget satisfied (each wolf-* module ≤ 4,000 tokens)
- [ ] HOOK-02 satisfied (all 7 consumers import through barrel without changes)
- [ ] COMPAT-01 satisfied (all 18 named exports + WorktreeContext type re-exported)
- [ ] COMPAT-02 satisfied (tsc --noEmit -p tsconfig.hooks.json exits 0 with no circular imports)
- [ ] ROADMAP.md and STATE.md updated to mark phase 2 complete
</success_criteria>

<output>
Create `.planning/phases/02-hook-module-split/02-03-SUMMARY.md` when done
</output>

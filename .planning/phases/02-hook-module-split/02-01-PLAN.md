---
phase: 02-hook-module-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/wolf-paths.ts
  - src/hooks/wolf-files.ts
  - src/hooks/wolf-json.ts
  - src/hooks/wolf-anatomy.ts
  - src/hooks/wolf-describe.ts
  - src/hooks/wolf-misc.ts
autonomous: true
requirements:
  - HOOK-01
  - COMPAT-01

must_haves:
  truths:
    - "Six new files exist in src/hooks/ with the wolf-* prefix, each containing the exact code extracted from the corresponding line range of the original shared.ts"
    - "wolf-describe.ts is shrunk per D-04 (PHP/Java/Kotlin/C# specifics/Ruby/Swift/Dart/Vue/Svelte/Astro/CSS/SQL/Proto/GraphQL/YAML/TOML/second Elixir/Lua/Zig branches removed) and includes the file header noting it is intentionally limited"
    - "Each wolf-* module has the imports its dependencies require (e.g. wolf-paths imports from ./worktree-helper.js, wolf-files imports from ./wolf-paths.js and ./wolf-json.js)"
    - "Internal helper isPlainObject and deepMergeDefaults are present in wolf-json.ts but NOT exported"
  artifacts:
    - path: "src/hooks/wolf-paths.ts"
      provides: "Path resolution + worktree context (getWolfDir, getSessionDir, getWorktreeContext, normalizePath)"
      min_lines: 60
      exports: ["getWolfDir", "getSessionDir", "getWorktreeContext", "normalizePath"]
    - path: "src/hooks/wolf-files.ts"
      provides: "File/dir/markdown utilities (ensureWolfDir, isWolfFile, readMarkdown, appendMarkdown, ensureSessionDir)"
      min_lines: 60
      exports: ["ensureWolfDir", "isWolfFile", "readMarkdown", "appendMarkdown", "ensureSessionDir"]
    - path: "src/hooks/wolf-json.ts"
      provides: "JSON I/O (readJSON, writeJSON) with internal isPlainObject/deepMergeDefaults"
      min_lines: 80
      exports: ["readJSON", "writeJSON"]
    - path: "src/hooks/wolf-anatomy.ts"
      provides: "anatomy.md parse/serialize (AnatomyEntry type, parseAnatomy, serializeAnatomy)"
      min_lines: 50
      exports: ["AnatomyEntry", "parseAnatomy", "serializeAnatomy"]
    - path: "src/hooks/wolf-describe.ts"
      provides: "Compact hook-relevant description extractor (extractDescription)"
      min_lines: 200
      max_lines: 320
      exports: ["extractDescription"]
    - path: "src/hooks/wolf-misc.ts"
      provides: "Token/time/stdin helpers (estimateTokens, timestamp, timeShort, readStdin)"
      min_lines: 20
      exports: ["estimateTokens", "timestamp", "timeShort", "readStdin"]
  key_links:
    - from: "src/hooks/wolf-paths.ts"
      to: "src/hooks/worktree-helper.ts"
      via: "named imports for detectWorktreeContextRaw, isNotARepoError, isMissingGitError, isTimeoutError, type WorktreeContext"
      pattern: "from \"./worktree-helper.js\""
    - from: "src/hooks/wolf-files.ts"
      to: "src/hooks/wolf-paths.ts"
      via: "imports getWolfDir, normalizePath"
      pattern: "from \"./wolf-paths.js\""
    - from: "src/hooks/wolf-files.ts"
      to: "src/hooks/wolf-json.ts"
      via: "imports writeJSON (for ensureSessionDir)"
      pattern: "from \"./wolf-json.js\""
---

<objective>
Create the six new wolf-* sibling modules in src/hooks/ by extracting code from
the current shared.ts (753 lines) per D-01, D-04, D-05, and D-09.

Purpose: This plan lays the foundation for the barrel facade in plan 02-02.
Each new module is a leaf (no imports from other wolf-* modules except for
the two specified edges) so that the barrel can wire them all up without
introducing cycles.

Output: Six new files in src/hooks/ (wolf-paths.ts, wolf-files.ts, wolf-json.ts,
wolf-anatomy.ts, wolf-describe.ts, wolf-misc.ts) — each containing the
extracted code with module-local imports correctly set up.

DOES NOT TOUCH shared.ts in this plan. The barrel facade is created in 02-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-hook-module-split/02-CONTEXT.md
@.planning/phases/02-hook-module-split/02-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md

# Source files (read in full to understand structure)
@src/hooks/shared.ts
@src/hooks/worktree-helper.ts
@src/hooks/shared.test.ts
@tsconfig.hooks.json
@package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create wolf-paths.ts, wolf-json.ts, wolf-anatomy.ts, wolf-misc.ts (leaf modules)</name>
  <files>
    src/hooks/wolf-paths.ts
    src/hooks/wolf-json.ts
    src/hooks/wolf-anatomy.ts
    src/hooks/wolf-misc.ts
  </files>
  <read_first>
    - src/hooks/shared.ts (the source of the extracted code)
    - src/hooks/worktree-helper.ts (must be imported by wolf-paths.ts for detectWorktreeContextRaw and WorktreeContext type)
    - .planning/phases/02-hook-module-split/02-CONTEXT.md (D-01, D-05, D-09 — placement rules)
    - .planning/phases/02-hook-module-split/02-RESEARCH.md (Export Map table for line ranges and target modules)
  </read_first>
  <action>
    Create four new files. Each one is a leaf module: it imports only from
    node:* built-ins and (for wolf-paths) the existing ./worktree-helper.js.

    **src/hooks/wolf-paths.ts** — Path resolution + worktree context.
    - Source: src/hooks/shared.ts lines 1-58 (worktree context cache + helpers)
      + lines 751-753 (normalizePath moved here per D-09).
    - Imports: `node:fs`, `node:path`, `node:crypto` (only if isWolfFile needs it —
      it does NOT; remove crypto if unused), plus named imports from
      `./worktree-helper.js`: detectWorktreeContextRaw, isMissingGitError,
      isNotARepoError, isTimeoutError, type WorktreeContext.
    - Top-level `let _cachedWorktreeCtx: WorktreeContext | null = null;` (private cache).
    - Function `detectWorktreeContext(): WorktreeContext` (private, NOT exported) —
      the body currently lives at lines 14-43 of shared.ts. Copy it verbatim.
    - Exports: `getWolfDir` (line 45), `getSessionDir` (line 50),
      `getWorktreeContext` (line 56), `normalizePath` (line 751). Copy each
      function body verbatim from shared.ts.
    - DO NOT export WorktreeContext type — re-export it via
      `export type { WorktreeContext };` at the bottom of the file
      (per RESEARCH.md Pitfall 6).
    - DO NOT include `ensureSessionDir` here (moved to wolf-files.ts per D-09).
    - DO NOT include `ensureWolfDir` or `isWolfFile` (those go to wolf-files.ts).
    - DO NOT import from any wolf-* module.

    **src/hooks/wolf-json.ts** — JSON read/write with internal helpers.
    - Source: src/hooks/shared.ts lines 117-196.
    - Imports: `node:fs`, `node:path`, `node:crypto`. No wolf-* imports.
    - Function `isPlainObject` (line 117) — private, NOT exported (D-05).
    - Function `deepMergeDefaults` (line 132) — private, NOT exported (D-05).
    - Exports: `readJSON` (line 149), `writeJSON` (line 166). Copy bodies
      verbatim from shared.ts.

    **src/hooks/wolf-anatomy.ts** — anatomy.md parse/serialize.
    - Source: src/hooks/shared.ts lines 219-272.
    - Imports: none (pure string functions). If TS complains about unused
      imports, do not add any.
    - Exports: `AnatomyEntry` (interface, line 219), `parseAnatomy` (line 225),
      `serializeAnatomy` (line 249). Copy bodies verbatim from shared.ts.

    **src/hooks/wolf-misc.ts** — Token/time/stdin helpers.
    - Source: src/hooks/shared.ts lines 726-749.
    - Imports: none (uses global `process` for stdin). Do NOT add `node:*` imports.
    - Exports: `estimateTokens` (line 726), `timestamp` (line 731),
      `timeShort` (line 735), `readStdin` (line 740). Copy bodies verbatim.

    **Critical rules:**
    - No fences, no prose — just create the file with the extracted TypeScript
      code. Each function body MUST be character-for-character identical to
      shared.ts (the only exception is wolf-describe.ts in Task 2, which has
      documented shrinkage).
    - Preserve all comments, including the JSDoc on `ensureWolfDir`
      (line 79-82) — wait, that one stays in wolf-files.ts. Preserve the
      JSDoc on `deepMergeDefaults` (line 126-131) — stays in wolf-json.ts.
    - Preserve `_cachedWorktreeCtx` exactly as `let` (mutable) — the
      test suite at shared.test.ts:181-208 verifies caching behavior.
    - The `export type { WorktreeContext };` line in wolf-paths.ts MUST be
      present (D-09 + RESEARCH.md Pitfall 6).
  </action>
  <acceptance_criteria>
    - `src/hooks/wolf-paths.ts` exists and contains the literal text `export function getWolfDir()`,
      `export function getSessionDir()`, `export function getWorktreeContext()`,
      `export function normalizePath(`, `export type { WorktreeContext };`,
      and `detectWorktreeContextRaw` (in an import statement from `./worktree-helper.js`).
    - `src/hooks/wolf-paths.ts` does NOT contain the strings `ensureSessionDir`, `ensureWolfDir`,
      or `isWolfFile` (those moved to wolf-files.ts).
    - `src/hooks/wolf-paths.ts` line count is between 60 and 130.
    - `src/hooks/wolf-json.ts` exists and contains `export function readJSON(`, `export function writeJSON(`,
      and the literal `isPlainObject` and `deepMergeDefaults` function bodies.
    - `src/hooks/wolf-json.ts` does NOT contain `export function isPlainObject` or
      `export function deepMergeDefaults` (D-05: internal-only).
    - `src/hooks/wolf-anatomy.ts` exists and contains `export interface AnatomyEntry`,
      `export function parseAnatomy(`, `export function serializeAnatomy(`.
    - `src/hooks/wolf-misc.ts` exists and contains all four exports
      (`estimateTokens`, `timestamp`, `timeShort`, `readStdin`).
    - None of the four files import from `./shared.js` or any other wolf-* module
      (only wolf-files.ts in Task 2 will import from wolf-paths/wolf-json).
    - `tsc --noEmit -p tsconfig.hooks.json` exits 0 (these are the only four files
      touched in this task; wolf-files.ts and wolf-describe.ts are created in Task 2
      so a tsc check now will fail because `ensureWolfDir`/`isWolfFile` are
      referenced from shared.ts but not yet extracted — defer the tsc check to Task 2).
  </acceptance_criteria>
  <verify>
    <automated>ls src/hooks/wolf-paths.ts src/hooks/wolf-json.ts src/hooks/wolf-anatomy.ts src/hooks/wolf-misc.ts && grep -c '^export ' src/hooks/wolf-paths.ts src/hooks/wolf-json.ts src/hooks/wolf-anatomy.ts src/hooks/wolf-misc.ts]<]minimax[>[</automated>
  </verify>
  <done>
    Four new files exist with the correct exports and no cross-wolf-* imports.
    wolf-paths.ts holds the worktree cache, getWolfDir, getSessionDir,
    getWorktreeContext, normalizePath, and re-exports the WorktreeContext type.
    wolf-json.ts holds readJSON, writeJSON, plus private isPlainObject and
    deepMergeDefaults. wolf-anatomy.ts holds AnatomyEntry, parseAnatomy,
    serializeAnatomy. wolf-misc.ts holds estimateTokens, timestamp, timeShort,
    readStdin.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create wolf-files.ts (with the only two cross-wolf-* imports) and wolf-describe.ts (with D-04 shrinkage)</name>
  <files>
    src/hooks/wolf-files.ts
    src/hooks/wolf-describe.ts
  </files>
  <read_first>
    - src/hooks/shared.ts (source for the extracted code; read both lines 60-115 and 198-217
      for wolf-files.ts, and lines 274-723 for wolf-describe.ts to identify the dropped branches)
    - src/hooks/wolf-paths.ts (created in Task 1 — must be read for the exact export names
      wolf-files.ts will import: getWolfDir, normalizePath)
    - src/hooks/wolf-json.ts (created in Task 1 — must be read for the exact export name
      wolf-files.ts will import: writeJSON)
    - .planning/phases/02-hook-module-split/02-RESEARCH.md §"Token Budget Resolution"
      and §"Shrinkage Plan" — exact line ranges to keep vs drop for wolf-describe.ts
    - .planning/phases/02-hook-module-split/02-CONTEXT.md D-04 (the language-branch drop list)
  </read_first>
  <action>
    Create two new files. wolf-files.ts is the only file with cross-wolf-*
    imports (per D-09: it imports from wolf-paths and wolf-json to break
    the cycle that would occur if `ensureSessionDir` lived in wolf-paths).
    wolf-describe.ts is the shrunk hook-relevant subset per D-04.

    **src/hooks/wolf-files.ts** — File/dir/markdown utilities.
    - Source: src/hooks/shared.ts lines 60-77 (ensureSessionDir), 79-88
      (ensureWolfDir + JSDoc), 90-115 (isWolfFile), 198-217 (readMarkdown,
      appendMarkdown).
    - Imports: `node:fs`, `node:path`, plus `getWolfDir` and `normalizePath`
      from `./wolf-paths.js`, plus `writeJSON` from `./wolf-json.js`.
    - Function `ensureSessionDir` (lines 60-77 of original) — copy verbatim.
      This function calls `getSessionDir()` (which is in wolf-paths.ts) and
      `writeJSON` (in wolf-json.ts). Place it FIRST in the file so the
      dependencies are clearly imported.
    - Function `ensureWolfDir` (lines 83-88) with its JSDoc (lines 79-82) — copy verbatim.
    - Function `isWolfFile` (lines 90-115) — copy verbatim.
    - Function `readMarkdown` (lines 198-211) — copy verbatim.
    - Function `appendMarkdown` (lines 213-217) — copy verbatim.
    - Exports: `ensureSessionDir`, `ensureWolfDir`, `isWolfFile`,
      `readMarkdown`, `appendMarkdown`. (NOT normalizePath — that lives in
      wolf-paths.ts per D-09.)
    - DO NOT export `_cachedWorktreeCtx`, `detectWorktreeContext`, or any
      worktree-helper types (those live in wolf-paths.ts).

    **src/hooks/wolf-describe.ts** — Compact, hook-relevant description extractor.
    - Source: src/hooks/shared.ts lines 274-723, SHRUNK per D-04.
    - The function `extractDescription` (signature: `export function extractDescription(filePath: string): string`)
      is the only export.
    - Imports: `node:fs`, `node:path`. No wolf-* imports.
    - MANDATORY SHRINKAGE: drop the following source ranges (in order — copy
      this drop list verbatim into a comment block in the file for traceability):
        * Lines 383-431: PHP / Laravel block
        * Lines 542-555: Java / Spring block
        * Lines 557-565: Kotlin block
        * Lines 567-581: C# / .NET (controller/DbContext specifics)
        * Lines 583-600: Ruby / Rails block
        * Lines 602-614: Swift block
        * Lines 616-624: Dart / Flutter block
        * Lines 626-636: Vue / Svelte / Astro block
        * Lines 638-646: CSS / SCSS / Less block
        * Lines 648-653: SQL block
        * Lines 655-667: Proto / GraphQL block
        * Lines 669-683: YAML block
        * Lines 685-689: TOML block
        * Lines 691-699: Elixir (Phoenix-specific second occurrence; the
          @moduledoc block at lines 361-365 is KEPT)
        * Lines 701-705: Lua block
        * Lines 707-711: Zig block
    - KEEP: lines 274-381 (foundation + Markdown + HTML + JSDoc + Python
      docstring + Rust doc comments + Go package comment + C# XML doc +
      Elixir @moduledoc + header-comment fallback), 433-540 (TS/JS/React/Next.js
      + Python/Django/FastAPI/Flask + Go HTTP handlers + Rust pub items),
      713-723 (last-resort generic decl-finder).
    - File header (replace the `known` table to-string with the
      intentionally-limited comment block per RESEARCH.md §"Shrinkage
      implementation rules"): the file must start with a JSDoc block
      describing it as the compact hook-relevant subset, naming the
      dropped languages, and pointing to
      `src/scanner/description-extractor.ts` for the full implementation.
    - The `known` Record (lines 278-292) and the file-read 12KB block
      (lines 295-304) and the `cap` helper (line 307) are KEPT — they
      are language-agnostic foundation code.
    - Post-shrinkage line count: target ~230 lines (down from 451). Token
      count target: ~3,000 (under the 4,000 HOOK-01 budget).
    - DO NOT keep a `known` entry for `composer.json` / `Gemfile` /
      `pubspec.yaml` in the KEEP set — actually, these are in the
      foundation (lines 278-292) and serve the early-return path for
      filenames. The D-04 drop list targets the EXTENSION-specific
      branches (lines 383+), not the filename lookup table. KEEP the
      `known` record as-is.

    **Verification for this task (after both files are written):**
    Run `tsc --noEmit -p tsconfig.hooks.json` and confirm exit code 0.
    This is the first time the build is exercised with the new modules
    in place (shared.ts still exports the old names — the barrel rewrite
    is plan 02-02's job — so tsc may show duplicate-export errors. If it
    does, that is expected and will be fixed in 02-02. The point of this
    verify is to catch syntax errors in the new files, not to prove
    end-to-end build success.)

    The acceptance criteria below only require that the new files
    contain the right shape — they do NOT require tsc to pass (that's
    plan 02-02's gate).
  </action>
  <acceptance_criteria>
    - `src/hooks/wolf-files.ts` exists and contains `export function ensureSessionDir(`, `export function ensureWolfDir(`, `export function isWolfFile(`, `export function readMarkdown(`, `export function appendMarkdown(`.
    - `src/hooks/wolf-files.ts` contains the literal strings `from "./wolf-paths.js"` and `from "./wolf-json.js"` (cross-module imports per D-09).
    - `src/hooks/wolf-files.ts` does NOT contain `export function normalizePath` (D-09: normalizePath lives in wolf-paths.ts).
    - `src/hooks/wolf-describe.ts` exists, starts with a JSDoc block naming the dropped languages and pointing to `src/scanner/description-extractor.ts`.
    - `src/hooks/wolf-describe.ts` contains `export function extractDescription(`.
    - `src/hooks/wolf-describe.ts` does NOT contain the string `PHP / Laravel` (the dropped branch header is gone).
    - `src/hooks/wolf-describe.ts` does NOT contain the string `Ruby / Rails` (the dropped branch header is gone).
    - `src/hooks/wolf-describe.ts` does NOT contain the string `Java / Spring` (the dropped branch header is gone).
    - `src/hooks/wolf-describe.ts` does NOT contain the string `Kotlin` (the dropped branch header is gone).
    - `src/hooks/wolf-describe.ts` line count is between 200 and 320 (post-shrinkage target ~230).
    - `src/hooks/wolf-describe.ts` still contains the `// ─── TS/JS/React/Next.js ─` section header (KEEP list) and the `// Last resort` block at the end (KEEP list).
    - `tsc --noEmit -p tsconfig.hooks.json` exits 0 OR exits with errors that are exclusively duplicate-export or unresolved-export errors in shared.ts (i.e. the new files themselves have no syntax/type errors). If the only errors are `TS2305` / `TS2308` on shared.ts re-exports, that is acceptable for this plan.
  </acceptance_criteria>
  <verify>
    <automated>wc -l src/hooks/wolf-files.ts src/hooks/wolf-describe.ts && grep -c 'PHP / Laravel\|Ruby / Rails\|Java / Spring\|Kotlin' src/hooks/wolf-describe.ts]<]minimax[>[</automated>
  </verify>
  <done>
    wolf-files.ts holds ensureSessionDir, ensureWolfDir, isWolfFile, readMarkdown,
    appendMarkdown, with imports from wolf-paths.ts (getWolfDir, normalizePath) and
    wolf-json.ts (writeJSON). wolf-describe.ts holds the shrunk extractDescription
    (~230 lines) with the file header documenting the dropped languages and the
    pointer to description-extractor.ts. No syntax errors in either new file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook runtime ↔ filesystem | Hooks read/write files in .wolf/ — same as before; refactor doesn't change this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-Split-01 | Tampering | wolf-describe.ts shrinkage | mitigate | The shrinkage is documented in D-04 and the file header points to the full version in `src/scanner/description-extractor.ts`. The scanner's full version is reapplied on the next anatomy scan, so the hook's degraded descriptions for rare languages are backfilled automatically. |
| T-02-Split-02 | Information Disclosure | Module placement | accept | The split is internal to the hook build; the public surface (barrel) preserves all 18 named exports. No new surface area is exposed (D-03). |
| T-02-Split-03 | Circular dependency | wolf-* internal imports | mitigate | D-09 explicitly places `ensureSessionDir` in `wolf-files.ts` and `normalizePath` in `wolf-paths.ts` to keep the internal graph acyclic. Plan 02-02's `tsc --noEmit -p tsconfig.hooks.json` check (COMPAT-02) catches any cycle at compile time under `module: Node16`. |
| T-02-Split-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. |

## Trust Boundary Notes

This plan introduces no new code paths, no new I/O, no new authentication, and
no new external dependencies. The threat surface is identical to the current
monolithic shared.ts.
</threat_model>

<verification>
- All six wolf-* files exist in src/hooks/
- Each new file's exports match the Export Map in 02-RESEARCH.md
- wolf-describe.ts is shrunk per D-04 (PHP/Java/Kotlin/C#/Ruby/Swift/Dart/Vue/Svelte/Astro/CSS/SQL/Proto/GraphQL/YAML/TOML/second Elixir/Lua/Zig blocks removed)
- wolf-files.ts is the only file with cross-wolf-* imports
- No new package installs; tsc strict mode may flag shared.ts re-exports as duplicate — that's expected and addressed in plan 02-02
</verification>

<success_criteria>
- [ ] src/hooks/wolf-paths.ts, wolf-files.ts, wolf-json.ts, wolf-anatomy.ts, wolf-describe.ts, wolf-misc.ts all exist
- [ ] Export counts and names match the Export Map in 02-RESEARCH.md
- [ ] wolf-describe.ts is ~230 lines, ≤ 4,000 tokens
- [ ] wolf-paths.ts re-exports the WorktreeContext type (D-09 / Pitfall 6)
- [ ] wolf-files.ts imports from wolf-paths.ts and wolf-json.ts (D-09)
- [ ] isPlainObject and deepMergeDefaults are NOT exported (D-05)
- [ ] No new public surface area (D-03) — all exports are subset of original shared.ts's exports
</success_criteria>

<output>
Create `.planning/phases/02-hook-module-split/02-01-SUMMARY.md` when done
</output>

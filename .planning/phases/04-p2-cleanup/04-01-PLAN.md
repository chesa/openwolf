---
phase: 04-p2-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02

must_haves:
  truths:
    - "Running `pnpm clean` exits 0 and removes dist/, .wolf/designqc-captures/ (when present), and any tmp.* directories"
    - "Running `pnpm clean` does NOT remove .wolf/memory.md, .wolf/cerebrum.md, or any other .wolf/ state files"
    - ".DS_Store files at repo root and .claude/ are no longer present on disk"
    - "The existing .gitignore bare `DS_Store` entry continues to prevent future .DS_Store commits"
  artifacts:
    - path: "package.json"
      provides: "clean script in scripts object"
      contains: "\"clean\":"
    - path: ".gitignore"
      provides: "DS_Store exclusion (pre-existing, unchanged)"
      contains: ".DS_Store"
  key_links:
    - from: "package.json scripts.clean"
      to: "dist/, .wolf/designqc-captures/, tmp.*"
      via: "node -e inline script using fs.existsSync guards + fs.rmSync"
      pattern: "existsSync.*rmSync"
---

<objective>
Add `pnpm clean` developer script to package.json and delete two untracked .DS_Store
files from the repository filesystem.

Purpose: Close the two remaining Phase 4 hygiene items — CLEAN-01 (clean script) and
CLEAN-02 (.DS_Store removal) — completing the maintenance sprint.

Output:
- package.json with new "clean" script entry (CLEAN-01, per D-01/D-02/D-04)
- .DS_Store and .claude/.DS_Store deleted from disk (CLEAN-02, per D-05)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-p2-cleanup/04-CONTEXT.md
@.planning/phases/04-p2-cleanup/04-RESEARCH.md
@.planning/phases/04-p2-cleanup/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pnpm clean script to package.json (CLEAN-01)</name>

  <read_first>
    package.json — read the full scripts object before editing; the prebuild script on
    line 10 is the canonical pattern to replicate. Confirm current script keys so the
    "clean" entry is inserted without disturbing any existing entry.
  </read_first>

  <files>package.json</files>

  <action>
    Add a "clean" key to the scripts object in package.json. The value must follow the
    exact same node -e inline pattern established by the prebuild script (D-01, D-03).

    The script must (D-02, D-04):
    - Remove dist/ using fs.existsSync guard before fs.rmSync
    - Remove .wolf/designqc-captures/ using fs.existsSync guard before fs.rmSync
    - Discover and remove all tmp.* directories via fs.readdirSync('.').filter with
      a regex that matches the literal string "tmp." at the start of the filename
    - NEVER glob or operate on .wolf/ root or .wolf/* — only the explicit subpath
      .wolf/designqc-captures/ is permitted

    Reference implementation from 04-PATTERNS.md (validate quoting with a smoke-test):
      "clean": "node -e \"const fs=require('fs');['dist','.wolf/designqc-captures'].forEach(p=>{if(fs.existsSync(p))fs.rmSync(p,{recursive:true,force:true})});fs.readdirSync('.').filter(f=>/^tmp\\./.test(f)).forEach(d=>fs.rmSync(d,{recursive:true,force:true}))\""

    Regex escaping note (RESEARCH.md Pitfall 2): inside the JSON string, the dot in
    /^tmp\./ must be written as \\. (double-backslash) so the JS runtime receives the
    correct escaped dot. Verify the regex matches tmp.7Djh6LTePQ and does not match
    tmpfoo by running the script after insertion.

    Placement: add "clean" after "test:watch" or after "prebuild" for logical grouping.
    Do NOT modify the prebuild entry (D-03).
  </action>

  <verify>
    <automated>
      node -e "const pkg=JSON.parse(require('fs').readFileSync('package.json','utf8'));if(!pkg.scripts.clean)throw new Error('clean script missing');console.log('PASS: clean script present:',pkg.scripts.clean)"
    </automated>
    <automated>
      pnpm clean 2>&1; echo "exit=$?"
    </automated>
    <automated>
      node -e "const fs=require('fs');if(fs.existsSync('dist'))throw new Error('dist/ still exists after clean');console.log('PASS: dist/ removed')" 2>/dev/null || echo "SKIP: dist/ did not exist before clean"
    </automated>
    <automated>
      node -e "const fs=require('fs');const tmps=fs.readdirSync('.').filter(f=>/^tmp\\./.test(f));if(tmps.length>0)throw new Error('tmp.* dirs remain: '+tmps.join(','));console.log('PASS: no tmp.* dirs remain')"
    </automated>
    <automated>
      node -e "const fs=require('fs');if(!fs.existsSync('.wolf'))throw new Error('.wolf/ missing after clean — CRITICAL: clean deleted wolf state');console.log('PASS: .wolf/ dir intact')"
    </automated>
  </verify>

  <acceptance_criteria>
    - package.json scripts object contains a "clean" key
    - pnpm clean exits 0
    - After pnpm clean: dist/ directory is removed (if it existed)
    - After pnpm clean: tmp.7Djh6LTePQ/ (and any other tmp.* directories) are removed
    - After pnpm clean: .wolf/ directory and its contents remain intact
    - After pnpm clean: .wolf/designqc-captures/ is removed only if it existed (no ENOENT)
    - The prebuild script entry in package.json is unchanged
    - The clean script uses node -e with fs built-ins and no new npm dependencies
  </acceptance_criteria>

  <done>
    package.json contains "clean" script. pnpm clean runs to exit 0, removes dist/ and
    tmp.* artifacts, and leaves .wolf/ state untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete .DS_Store files from disk (CLEAN-02)</name>

  <read_first>
    .planning/phases/04-p2-cleanup/04-CONTEXT.md — read D-05 and D-06 to confirm:
    (1) both .DS_Store files are untracked (no git rm --cached needed), and
    (2) .gitignore requires no change.
    .gitignore — confirm the bare "DS_Store" entry is still present before closing the task.
  </read_first>

  <files>
    .DS_Store (deleted),
    .claude/.DS_Store (deleted)
  </files>

  <action>
    Delete both .DS_Store files from disk using rm (D-05). These files are physically
    present but NOT tracked by git (confirmed in RESEARCH.md: git ls-files returns nothing
    for both paths). Do NOT run git rm --cached — that would error since the files are
    untracked.

    Commands to run:
      rm .DS_Store
      rm .claude/.DS_Store

    After deletion: run git status to confirm there are no staged changes or unexpected
    modifications. The .gitignore bare "DS_Store" entry already covers root and all
    subdirectories — no .gitignore edit is required (D-06).

    Note: because these files are untracked and .gitignore already excludes them, git
    status will show no changes after deletion. There is no commit for this task — the
    files simply no longer exist on disk.
  </action>

  <verify>
    <automated>
      test ! -f .DS_Store && echo "PASS: .DS_Store removed" || echo "FAIL: .DS_Store still present"
    </automated>
    <automated>
      test ! -f .claude/.DS_Store && echo "PASS: .claude/.DS_Store removed" || echo "FAIL: .claude/.DS_Store still present"
    </automated>
    <automated>
      grep -c "DS_Store" .gitignore
    </automated>
    <automated>
      git status --short | grep "DS_Store" && echo "UNEXPECTED: DS_Store in git status" || echo "PASS: no DS_Store in git status (correctly untracked)"
    </automated>
  </verify>

  <acceptance_criteria>
    - .DS_Store does not exist at repo root (test -f .DS_Store returns non-zero)
    - .claude/.DS_Store does not exist (test -f .claude/.DS_Store returns non-zero)
    - .gitignore still contains "DS_Store" entry (unchanged)
    - git status shows no DS_Store-related changes (files were untracked, deletion is invisible to git)
    - No git rm --cached was run (would have errored on untracked files)
  </acceptance_criteria>

  <done>
    Both .DS_Store files are absent from disk. .gitignore bare "DS_Store" entry is
    unchanged. git status is clean of any .DS_Store reference.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell -> pnpm clean | Script is run in the project working directory; operates only on local filesystem |
| package.json scripts -> node -e | Inline JS executes with developer's file permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | package.json clean script path list | mitigate | Script uses explicit hardcoded paths (dist/, .wolf/designqc-captures/) and a regex-filtered readdirSync — not a glob on .wolf/ root; locked by D-02 |
| T-04-02 | Denial of Service | pnpm clean deletes .wolf/ state | mitigate | Acceptance criteria include explicit post-clean check that .wolf/ directory and contents remain intact; D-02 prohibits .wolf/ root globbing |
| T-04-03 | Information Disclosure | .DS_Store files expose Finder metadata | accept | Files are untracked and will be deleted; .gitignore already prevents future commits; no code or credential data in .DS_Store |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase; Package Legitimacy Audit section in RESEARCH.md confirms zero new installs |
</threat_model>

<verification>
After both tasks complete:

1. pnpm clean exits 0 (no errors)
2. .DS_Store absent at repo root
3. .claude/.DS_Store absent
4. package.json scripts.clean key present
5. .wolf/ directory and all state files intact after running pnpm clean
6. git status is clean (Task 2 produces no git diff; Task 1 produces one modified file)
7. pnpm build still succeeds (prebuild script unchanged per D-03)
</verification>

<success_criteria>
Phase 4 is complete when:
- `pnpm clean` removes dist/, .wolf/designqc-captures/, and tmp.* directories and exits 0 (CLEAN-01)
- `pnpm clean` does NOT delete .wolf/ state files (CLEAN-01 safety constraint)
- Both .DS_Store files are absent from disk (CLEAN-02)
- .gitignore bare DS_Store entry is still present and unchanged (CLEAN-02)
- The maintenance sprint is 100% complete: all 9 plans across phases 1-4 done
</success_criteria>

<output>
Create `.planning/phases/04-p2-cleanup/04-01-SUMMARY.md` when done.
</output>

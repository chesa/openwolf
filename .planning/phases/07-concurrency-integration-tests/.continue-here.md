---
context: phase
phase: 07-concurrency-integration-tests
task: 0
total_tasks: 3
status: not_started
last_updated: 2026-06-24T15:45:01.518Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: in-process concurrency tautology — The original imported plan's `concurrency.test.ts` mocked `withFileLock` to a no-op and ran a single in-process merge, so it would pass even with the lock deleted (exactly the trap documented in `docs/superpowers/2026-06-23-shared-checkout-concurrency-phase1-LEARNINGS.md`). The plan was revised to fix this. When executing Task 1, write the test EXACTLY as the revised plan specifies (file is `merge-accumulation.test.ts`, asserts `withFileLock` was invoked). Do NOT "improve" it back into a naive concurrency test, and do NOT remove the `withFileLock`-called assertion — that assertion is what verifies MERGE-02.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| In-process concurrency tautology | A "concurrency" test that mocks the lock to a no-op and runs synchronously passes with or without the lock — proves nothing | blocking | Follow the revised plan: name the file `merge-accumulation.test.ts`, keep `expect(vi.mocked(withFileLock)).toHaveBeenCalled()`, and keep the "Concurrency Coverage Rationale" section's reasoning intact |
| Pre-declared test results in SUMMARY | Copying "3/3 passed" from the plan template into SUMMARY without running | advisory | Fill SUMMARY Verification counts from ACTUAL run output (the plan template now says `<N>/<N>` for this reason) |

_The discuss-phase and execute-phase workflows parse this table and enforce a mandatory understanding check for any `blocking` rows._

<current_state>
Milestone v1.1 (Shared-Checkout Concurrency — Pillar C). Phases 5 & 6 are complete and committed; Phase 7 is the final phase. Phase 7's plan (`07-01-PLAN.md`) has been imported from a freeform superpowers doc AND revised after a code review against the phase-1 concurrency learnings. **No Phase 7 code has been written yet** — the plan is ready to execute. This is the last phase before v1.1 ships.

`.planning/` health: HEALTHY (0 errors, 0 warnings; only I001 info = this phase has no SUMMARY yet, which is expected and clears on execution).
</current_state>

<completed_work>

This session was all planning/setup for Phase 7 — no plan tasks executed yet. Four commits on `develop`:

- `067e3b1` — Imported Phase 7 plan from `docs/superpowers/plans/2026-06-24-concurrency-integration-tests.md` (freeform → GSD `07-01-PLAN.md`). Conflict detection: 0 blockers, 1 approved warning.
- `18b0131` — Revised the imported plan after a code review against the phase-1 LEARNINGS: renamed `concurrency.test.ts` → `merge-accumulation.test.ts`, added a `withFileLock`-invoked assertion (verifies MERGE-02 in-process), added a "Concurrency Coverage Rationale" section, added the missing TEST-02 "session dir with no staging file" edge test (integration test is now 4 cases), softened an overclaimed `must_haves` truth, and corrected the inapplicable TDD red-phase framing (code already exists from Phase 6).
- `0993e54` — Reconciled ROADMAP/STATE: marked phases 5 & 6 complete (they were implemented but tracking lagged), repositioned to Phase 7. Verified Phase 6 first: `learnings.test.ts` ran 8/8 green. Cleared a false-positive W002 by rewording a historical "Phase 1" reference.
- `489b2f4` — Cleared health warnings: W001 (PROJECT.md `## Overview` → `## What This Is`), W019 (archived stale root `.continue-here.md` → `.planning/reports/`).
</completed_work>

<remaining_work>

Execute `07-01-PLAN.md` — 3 tasks, no production-code changes:
- Task 1 (not started): write `tests/cli/merge-accumulation.test.ts` (3 tests: multi-session accumulation, archival, empty-state) + the `withFileLock`-invoked assertion. Run (expect pass on first run — code already shipped). Commit.
- Task 2 (not started): write `tests/cli/learnings-integration.test.ts` (4 tests: multi-session enumeration, empty staging file, session dir with no staging file, missing sessions dir). Run. Commit.
- Task 3 (not started): `pnpm test` (all + 7 new), `pnpm build`, write `07-01-SUMMARY.md` (fill verification counts from ACTUAL output), commit.

After Phase 7: milestone v1.1 is complete → consider `/gsd-complete-milestone`.
</remaining_work>

<decisions_made>

- Phase 7 concurrency coverage is in-process accumulation + a `withFileLock`-called assertion, NOT a spawn-based cross-process e2e. Rationale: Pillar C's safety is structural (per-session staging files = no propose-path race; merge is a single human-driven command). The lock primitive itself is already cross-process-verified in `tests/e2e-concurrency.test.ts`. This is written into the plan's "Concurrency Coverage Rationale" section.
- Marked phases 5 & 6 complete in tracking only after verifying real evidence (committed source + Phase 6 tests 8/8 green), not just the presence of SUMMARY files.
- Archived (moved) the stale root handoff rather than deleting it — non-destructive default for a file not created this session.
</decisions_made>

<blockers>
- None functional. Phase 7's dependency (Phase 6 `learnings-cmd.ts`) is built, committed, and tested.
</blockers>

## Required Reading (in order)
1. `.planning/phases/07-concurrency-integration-tests/07-01-PLAN.md` — the revised plan to execute; read the "Concurrency Coverage Rationale" and Task 1's header note before writing any test.
2. `docs/superpowers/2026-06-23-shared-checkout-concurrency-phase1-LEARNINGS.md` — why the concurrency test must not be a tautology (the constraint above).
3. `src/cli/learnings-cmd.ts` — the code under test (`learningsCommand`, `learningsMergeCommand`); note where `withFileLock` is actually called.

## Critical Anti-Patterns (do NOT repeat these)
- ANTI-PATTERN: mocking `withFileLock` to a pure no-op with no assertion → reintroduces the tautology. Mitigation: keep the `toHaveBeenCalled()` assertion.

## Infrastructure State
- Branch: `develop` (4 WIP commits this session, all pushed locally only — not pushed to remote).
- Tests: Phase 6 `learnings.test.ts` 8/8 green (verified this session). Full suite not re-run since.
- Build: not re-run this session (no production code changed).

## Human actions pending (triage before/after Phase 7)
- The working tree has **71 uncommitted deletions** of old v1.0 phase dirs (`.planning/phases/00-prerequisite-fix/` … `04-p2-cleanup/`, plus `01-p0-security-fixes-quick-win/`, `02-hook-module-split/`, `03-p1-modularization/`, `03-workflow-improvements/`). These predate this session and were left untouched. Decide: intentional cleanup (commit the deletions) or accidental (`git restore`)? They don't affect health (those phases are archived in `.planning/milestones/`).
- Untracked reference files worth a decision: `docs/superpowers/2026-06-23-...-LEARNINGS.md` and `docs/superpowers/plans/2026-06-24-concurrency-integration-tests.md` (keep/commit), `.planning/reports/20260623-session-report.md` (keep), `PR#17.md` + `PR#18.md` (stale review artifacts — safe to delete).

<context>
The work this session was entirely about getting Phase 7's plan right BEFORE executing it — importing it, then hardening it against a known failure mode, then making the surrounding project state honest. The next session's job is mechanical execution of a well-vetted plan. The single thing that matters: write the tests as specified, especially the lock assertion. Everything else is green and clear.
</context>

<next_action>
Start with: `/gsd-execute-phase 07` (or subagent-driven-development per the plan's header). Task 1 first — write `tests/cli/merge-accumulation.test.ts` exactly per the revised plan, including `expect(vi.mocked(withFileLock)).toHaveBeenCalled()`. Expect tests to pass on first run (Phase 6 code already exists); a failure means a test-setup bug, not a production bug.
</next_action>

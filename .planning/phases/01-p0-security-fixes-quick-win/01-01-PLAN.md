---
phase: "01-p0-security-fixes-quick-win"
plan: "01"
wave: 1
depends_on: []
requirements_addressed:
  - SESS-01
  - SESS-02
files_modified:
  - src/daemon/cron-engine.ts
autonomous: true
---

<objective>
Fix consolidateMemory() to delete zero-action sessions entirely and never write "Consolidated session (0 actions)" marker entries.
Purpose: Eliminates false zero-action markers; memory.md stays clean with only sessions that have real history.
Output: Fixed consolidateMemory() in cron-engine.ts
</objective>

<context>
@src/daemon/cron-engine.ts
</context>

<tasks>

<task type="auto">
<name>Task 1: Fix consolidateMemory() zero-action deletion</name>
<files>src/daemon/cron-engine.ts</files>
<read_first>src/daemon/cron-engine.ts</read_first>
<action>
In src/daemon/cron-engine.ts, method consolidateMemory() (line 237):

1. Add a tracking variable before the for loop (after line 248):
   let currentSessionActionCount = 0;

2. When the for loop encounters a new `## Session:` header (line 252-270):
   - Before flushing the previous old session (lines 254-259), compute actionCount FROM oldSessionLines using the same filter.
   - IF currentSessionActionCount > 0: write the header (was pushed at line 265), session lines, and marker.
   - IF currentSessionActionCount === 0: do NOT write anything — skip header AND marker entirely.
   - Reset currentSessionActionCount = 0 and oldSessionLines = [] for the new session.

3. When accumulating oldSessionLines in the loop body (line 274), also count action rows:
   - After pushing to oldSessionLines, increment currentSessionActionCount if the line starts with "|" AND does not start with "|--" AND does not start with "| Time".

4. In the final flush at end of function (lines 281-285):
   - Use currentSessionActionCount (not recompute from oldSessionLines) to guard both the marker write and the preceding session header write.
   - If currentSessionActionCount === 0: skip entire session block (no header, no marker, no trailing empty line).

Specifically:
- Line 265: Move `result.push(line)` — the header push — INTO the flush guard so it only happens when flush confirms actionCount > 0.
- Rename the `actionCount` variable in the flush blocks to `currentSessionActionCount` to use the tracked value.
- Ensure the filter used to count action rows is identical in both flush sites:
  `l.startsWith("|") && !l.startsWith("|--") && !l.startsWith("| Time")`

Per D-01: Zero-action session blocks are deleted entirely from memory.md — no `## Session` header, no `> Consolidated session (0 actions)` marker. Only sessions with at least one action are preserved.
</action>
<acceptance_criteria>
1. memory.md consolidation skips session blocks where actionCount === 0 — no header line, no marker line, no empty lines for those sessions.
2. Sessions with actionCount > 0 are preserved as `> Consolidated session (N actions)` with N > 0.
3. Both flush sites (encountering new header and EOF) guard on the same tracked actionCount.
</acceptance_criteria>
<verify>
<automated>grep -E '^## Session:|^> Consolidated session \(0 actions\)' .wolf/memory.md 2>/dev/null; echo "Exit: $?"</automated>
</verify>
<done>ConsoleWolf session consolidation drops zero-action sessions; memory.md contains only sessions with actions > 0; no "Consolidated session (0 actions)" markers exist anywhere in the file.</done>
</task>

</tasks>

<success_criteria>
SESS-01: Daemon consolidator deletes zero-action sessions from memory.md entirely (no marker entries written)
SESS-02: Daemon consolidator never writes "Consolidated session (0 actions)" marker entries to memory.md
</success_criteria>
</objectives>

<output>
After execution, create .planning/phases/01-p0-security-fixes-quick-win/01-01-SUMMARY.md
</output>

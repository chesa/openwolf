---
phase: "01-p0-security-fixes-quick-win"
plan: "02"
type: plan
wave: 1
depends_on: []
requirements_addressed:
  - AUTH-01
  - AUTH-02
files_modified:
  - src/daemon/wolf-daemon.ts
  - src/dashboard/app/lib/wolf-client.ts
autonomous: true
must_haves:
  truths:
    - "WebSocket upgrade request sends token via Authorization: Bearer header (not URL query param)"
    - "Daemon verifyClient reads token from info.req.headers.authorization"
    - "Token does not appear in browser history, proxy logs, or server access logs"
    - "safeCompareToken validates token from header"
  artifacts:
    - path: src/daemon/wolf-daemon.ts
      provides: "WebSocket verifyClient reads Authorization header for token validation"
      min_lines: 25
    - path: src/dashboard/app/lib/wolf-client.ts
      provides: "WebSocket constructor sends Authorization: Bearer header on upgrade"
      min_lines: 20
  key_links:
    - from: src/dashboard/app/lib/wolf-client.ts
      to: src/daemon/wolf-daemon.ts
      via: "WebSocket upgrade with Authorization: Bearer header"
      pattern: "new WebSocket.*headers.*Authorization.*Bearer"
---

<objective>
Migrate dashboard WebSocket authentication from URL query param (?token=) to Authorization: Bearer header on the upgrade request.
Purpose: Removes token from browser history, proxy logs, and server access logs. Token stays in JS memory only.
Output: Updated wolf-daemon.ts verifyClient + wolf-client.ts WebSocket constructor
</objective>

<context>
@src/daemon/wolf-daemon.ts
@src/dashboard/app/lib/wolf-client.ts
</context>

<tasks>

<task type="auto">
<name>Task 1: Update wolf-daemon.ts verifyClient to parse Authorization header</name>
<files>src/daemon/wolf-daemon.ts</files>
<read_first>src/daemon/wolf-daemon.ts</read_first>
<action>
In src/daemon/wolf-daemon.ts, within the WebSocketServer verifyClient callback (lines 324-349):

1. Remove the URL parsing block (lines 334-338) that does:
   ```ts
   const wsUrl = new URL(info.req.url ?? "", `http://${info.req.headers.host ?? "localhost"}`);
   const token = wsUrl.searchParams.get("token") ?? "";
   ```

2. Replace with Authorization header parsing:
   ```ts
   const authHeader = (info.req.headers.authorization ?? "") as string;
   const token = authHeader.replace(/^Bearer\s+/i, "").trim();
   ```

3. Keep the `if (!safeCompareToken(token))` check and logger.warn messages unchanged.
   The `safeCompareToken` function (lines 80-92) is reused as-is — it does constant-time comparison and handles empty strings.

4. Update the comment above the token auth block (lines 330-333) from "parse ?token= from the WS upgrade URL" to something like "parse Authorization header from the WS upgrade request" and update the comment about dashboard appending token to note it uses the header instead.

Per D-02: WebSocket authentication uses Authorization: Bearer header on the upgrade request (not URL query param ?token=).
Per D-03: Single-origin SPA; CSRF not a concern; token kept in JS memory, out of URL and proxy logs.
</action>
<acceptance_criteria>
1. verifyClient reads Authorization header from info.req.headers.authorization (not URL searchParams).
2. Token is extracted by removing "Bearer " prefix (case-insensitive) and trimming whitespace.
3. safeCompareToken() is called with the extracted token — no other token acquisition logic.
4. logger.warn messages remain for invalid/missing token rejection.
</acceptance_criteria>
<verify>
<automated>grep -c 'info.req.headers.authorization' src/daemon/wolf-daemon.ts && grep -c 'searchParams.get("token")' src/daemon/wolf-daemon.ts</automated>
</verify>
<done>wolf-daemon.ts verifyClient parses Authorization header; no URL searchParams token parsing remains.</done>
</task>

<task type="auto">
<name>Task 2: Update wolf-client.ts WebSocket constructor to send Authorization header</name>
<files>src/dashboard/app/lib/wolf-client.ts</files>
<read_first>src/dashboard/app/lib/wolf-client.ts</read_first>
<action>
In src/dashboard/app/lib/wolf-client.ts, constructor (lines 9-16):

1. Remove the URL query param construction from line 15 (`this.url = token ? ...?token=...`).
2. Store `base` URL without the token appended:
   ```ts
   const base = url || `${wsProtocol}//${location.host}/ws`;
   this.url = base;
   ```

3. In the connect() method (line 20), construct the WebSocket differently based on whether token is available:
   - If token exists: `new WebSocket(base, { headers: { Authorization: \`Bearer ${token}\` } })`
   - If no token: `new WebSocket(base)`

   Note: Browser native WebSocket constructor accepts `headers` option as second argument for custom headers during handshake. No package changes needed.

   The token comes from the constructor parameter (passed from main.tsx, stored in sessionStorage). The Authorization header will be sent on every WebSocket handshake, keeping the token in memory only.

Per D-02: WebSocket authentication uses Authorization: Bearer header on the upgrade request.
Per D-03: Token is stored in sessionStorage on the client; only transport changes (URL param -> Authorization header).
</action>
<acceptance_criteria>
1. WebSocket constructor uses headers option with Authorization: Bearer {token} when token is provided.
2. No `?token=` appears in any URL construction in wolf-client.ts.
3. Token is read from constructor parameter (sessionStorage on reconnect flow).
4. If token is absent, WebSocket connects without Authorization header (for non-authed connections if needed).
</acceptance_criteria>
<verify>
<automated>grep -c '?token=' src/dashboard/app/lib/wolf-client.ts && grep -c 'Authorization.*Bearer' src/dashboard/app/lib/wolf-client.ts</automated>
</verify>
<done>wolf-client.ts sends Authorization: Bearer header on WebSocket upgrade; no token appears in any URL.</done>
</task>

</tasks>

<success_criteria>
AUTH-01: Dashboard WebSocket handshake authenticates via Authorization: Bearer header (not URL query param.Authorization header is read from info.req.headers.authorization)
AUTH-02: Proxy access logs contain no token value after auth migration (token moves from URL to header; proxies typically do not log request headers)
</success_criteria>
</objectives>

<output>
After execution, create .planning/phases/01-p0-security-fixes-quick-win/01-02-SUMMARY.md
</output>
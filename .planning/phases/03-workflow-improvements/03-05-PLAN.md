---
phase: 03-workflow-improvements
plan: 05
type: execute
wave: 4
depends_on: [03-01, 03-02, 03-03, 03-04]
files_modified:
  - docs/configuration.md
  - docs/getting-started.md
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "`docs/configuration.md` documents `OPENWOLF_METADATA_DIR`, `.wolf/.gitignore`, and file locking behavior"
    - "`docs/getting-started.md` covers the mixed commit strategy, concurrent write safety, and team workflow"
    - "Both docs are linked from `README.md`"
  artifacts:
    - path: "docs/configuration.md"
      provides: "Reference for all configuration options"
      contains: "OPENWOLF_METADATA_DIR"
      min_lines: 100
    - path: "docs/getting-started.md"
      provides: "Team onboarding walkthrough"
      contains: "mixed commit"
      min_lines: 80
  key_links:
    - from: "README.md"
      to: "docs/configuration.md"
      via: "Markdown link"
      pattern: "configuration.md"
    - from: "README.md"
      to: "docs/getting-started.md"
      via: "Markdown link"
      pattern: "getting-started.md"

---

<objective>
Update the reference and onboarding documentation to cover the new features from this phase: `OPENWOLF_METADATA_DIR`, `.wolf/.gitignore` mixed commit strategy, `withFileLock` concurrent write safety, and the team workflow for all of these.

Purpose: Team members need a reference for configuration options and a walkthrough for the mixed commit strategy. Reference docs and onboarding docs serve different readers and purposes, so they are split per D-05.
Output: Updated `docs/configuration.md` and `docs/getting-started.md`, both linked from `README.md`.
</objective>

<execution_context>
@/Users/bfs/.config/opencode/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-workflow-improvements/03-CONTEXT.md (D-03, D-04, D-05)
@.planning/phases/03-workflow-improvements/03-RESEARCH.md (Pitfalls, State of the Art)

# Existing docs to read
@docs/configuration.md
@docs/getting-started.md
@README.md

# Source files for feature verification
@src/hooks/wolf-paths.ts
@src/templates/.gitignore
@src/hooks/wolf-lock.ts
@src/cli/init.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/configuration.md with new configuration options</name>
  <files>docs/configuration.md</files>

  <read_first>
  - docs/configuration.md (full file — existing content to preserve and extend)
  - 03-CONTEXT.md D-03 (OPENWOLF_METADATA_DIR behavior), D-04 (.wolf/.gitignore), D-05 (docs scope)
  - src/hooks/wolf-lock.ts (for WITH_FILE_LOCK_TTL_MS env var)
  - src/templates/.gitignore (for template content)
  </read_first>

  <action>
  Update `docs/configuration.md` to add documentation for the three new features from this phase. Preserve all existing content and extend with the new sections below.

  **Section 1 — Add `OPENWOLF_METADATA_DIR` to the Environment Variables table:**
  Insert a new row after the `OPENWOLF_PROJECT_ROOT` row (or at an appropriate alphabetical position) in the existing table:

  ```markdown
  | `OPENWOLF_METADATA_DIR` | Optional | `<project>/.wolf/` | Absolute path to an alternate metadata storage location. If set, OpenWolf stores config, state, and knowledge files at this path instead of the project's `.wolf/` directory. Useful for shared network drives, dedicated volumes, or multi-project setups. Must be an absolute path — relative paths are rejected with a warning and fall back to the default. Hook scripts remain at `<project>/.wolf/hooks/`. |
  ```

  **Section 2 — Add `.wolf/.gitignore` subsection:**
  Add a new top-level section titled `### .wolf/.gitignore (mixed commit strategy)` after the existing configuration sections (before "Required vs optional settings" or after "Per-environment overrides" — use judgment based on logical flow). Content:

  ```markdown
  ### `.wolf/.gitignore` (mixed commit strategy)

  OpenWolf ships with a default `.wolf/.gitignore` template that implements a "mixed commit strategy": only configuration files are tracked in git, while session state and runtime data remain ignored.

  **Default template (`src/templates/.gitignore`):**

  ```
  *
  !.gitignore
  !OPENWOLF.md
  !identity.md
  !config.json
  ```

  - `*` — ignore everything in `.wolf/` by default
  - `!.gitignore` — track the gitignore itself so users who clone the repo get the correct ignore rules
  - `!OPENWOLF.md` — track the project context document (this is a manually curated file worth sharing)
  - `!config.json` — track the configuration (port assignments, scan intervals, exclude patterns)
  - `!identity.md` — track the project identity (name, description, creation date)

  To track additional files (e.g., `cerebrum.md` for team-wide learnings), add a `!` line to `.wolf/.gitignore`:

  ```gitignore
  !cerebrum.md
  ```

  To revert to the "ignore everything" behavior (the old approach), delete all `!` lines except `!.gitignore` — or simply add `.wolf/` to your project-root `.gitignore`.

  The template is overwritten on every `openwolf init` or `openwolf update`. If you customize it, re-apply your changes after upgrading.
  ```

  **Section 3 — Add `WITH_FILE_LOCK_TTL_MS` to the Environment Variables table:**
  Add another row:

  ```markdown
  | `WITH_FILE_LOCK_TTL_MS` | Optional | `30000` | Staleness threshold (milliseconds) for the advisory file lock used by concurrent hook processes. When a hook process crashes while holding a lock, the lock file remains. After this TTL expires, the lock is considered stale and automatically broken. Increase for very slow filesystems; decrease for faster lock recovery. |
  ```

  **Section 4 — Update the intro paragraph:**
  The current intro says "All settings have sensible defaults, so no configuration is required for normal use." This remains correct. Optionally expand the sentence to mention the new options: `OPENWOLF_METADATA_DIR` and `WITH_FILE_LOCK_TTL_MS` for advanced deployment scenarios.

  **Preservation rules:**
  - Keep ALL existing env var entries (OPENWOLF_PROJECT_ROOT, CLAUDE_PROJECT_DIR, PROGRAMFILES, LOCALAPPDATA, ANTHROPIC_API_KEY)
  - Keep ALL existing config file sections (anatomy, token_audit, cron, memory, cerebrum, daemon, dashboard, designqc)
  - Keep "Required vs optional settings", "Defaults", "Per-environment overrides" sections
  - Only ADD the new sections described above
  </action>

  <verify>
  <automated>grep -c "OPENWOLF_METADATA_DIR" docs/configuration.md && grep -c "WITH_FILE_LOCK_TTL_MS" docs/configuration.md && grep -c "mixed commit" docs/configuration.md</automated>
  </verify>

  <acceptance_criteria>
  - `docs/configuration.md` contains `OPENWOLF_METADATA_DIR` in the env var table with absolute-path requirement noted
  - `docs/configuration.md` contains `WITH_FILE_LOCK_TTL_MS` in the env var table with default 30000ms noted
  - `docs/configuration.md` contains a `.wolf/.gitignore (mixed commit strategy)` section with the template content
  - All existing content (other env vars, config sections) is preserved
  - No code blocks inside list items (markdown formatting requirement)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update docs/getting-started.md with mixed commit strategy and team workflow</name>
  <files>docs/getting-started.md</files>

  <read_first>
  - docs/getting-started.md (full file — existing content to preserve and extend)
  - 03-CONTEXT.md D-04 (.wolf/.gitignore), D-05 (doc split), D-01 (file locking)
  - src/templates/.gitignore (template content to reference)
  </read_first>

  <action>
  Update `docs/getting-started.md` to add the mixed commit strategy walkthrough and team workflow information. Preserve all existing content (installation steps, first run, common setup issues, next steps).

  **Add a new section after "First run" (before "Common setup issues"):**

  ```markdown
  ## Mixed commit strategy

  By default, OpenWolf uses a **mixed commit strategy** for `.wolf/`: configuration files are committed to git, while session state and runtime data stay ignored. This allows the team to share project context (`OPENWOLF.md`), tool configuration (`config.json`), and project identity (`identity.md`) without flooding every commit with session churn.

  ### What gets committed

  After `openwolf init`, the `.wolf/.gitignore` file permits these files:

  | File | Purpose |
  |------|---------|
  | `.wolf/.gitignore` | The ignore rules themselves — ensures correct behavior on clone |
  | `.wolf/OPENWOLF.md` | Project context document (manual curated) |
  | `.wolf/config.json` | Tool configuration (ports, scan intervals, exclude patterns) |
  | `.wolf/identity.md` | Project name, description, creation date |

  All other `.wolf/` files (`cerebrum.md`, `anatomy.md`, `memory.md`, `token-ledger.json`, `buglog.json`, session files, etc.) are ignored by default.

  ### Tracking additional files

  To share more files with the team (e.g., team-wide learnings in `cerebrum.md`), edit `.wolf/.gitignore` and add:

  ```gitignore
  !cerebrum.md
  ```

  Commit the updated `.wolf/.gitignore` so the whole team gets the change.

  ### Reverting to full ignore

  To go back to the traditional approach (`.wolf/` completely untracked), remove all `!` lines from `.wolf/.gitignore` except `!.gitignore`, or add `.wolf/` to your project-root `.gitignore`.

  ### Note for existing projects

  If you initialized OpenWolf before this feature was introduced, your project-root `.gitignore` may still have `.wolf/` appended from the previous version. The two ignores coexist without conflict — the project-root `.gitignore` takes precedence. To use the new mixed strategy, remove `.wolf/` from your project-root `.gitignore`. The `.wolf/.gitignore` template will take over automatically after `openwolf update`.
  ```

  **Add another section after the Mixed commit strategy (or after "Common setup issues"):**

  ```markdown
  ## Concurrent write safety

  OpenWolf hooks (session-start, pre-read, post-read, pre-write, post-write, stop) run as separate Node.js processes. When multiple hooks execute concurrently — for example, during parallel tool calls — they may write to the same `.wolf/` JSON files (`_session.json`, `token-ledger.json`, `buglog.json`).

  To prevent data corruption, OpenWolf uses **advisory per-file locking**:

  - Each write acquires an exclusive lock on the target file using `fs.openSync` with `O_CREAT | O_EXCL`
  - If another process holds the lock, the writer retries up to 10 times with 50ms backoff (500ms worst-case total)
  - If the lock is stale (process crash without cleanup), it is automatically detected and broken after 30 seconds
  - The staleness threshold is configurable via the `WITH_FILE_LOCK_TTL_MS` environment variable

  The locking is transparent — hooks continue to call `writeJSON()` through the `shared.ts` facade without any code changes. Only the write path is affected; reads are lock-free and never block.

  This feature requires no configuration for normal use. If you frequently see "Could not acquire lock" warnings in stderr, consider increasing `WITH_FILE_LOCK_TTL_MS` or investigating why hooks are racing on the same file.
  ```

  **If `README.md` doesn't already link to both docs:**
  Check `README.md` for links to `docs/configuration.md` and `docs/getting-started.md`. If either is missing, add them in the "Documentation" or "Next steps" section. Standard link format:

  ```markdown
  - [Configuration](docs/configuration.md) — Environment variables, config file format, and advanced settings
  - [Getting Started](docs/getting-started.md) — Installation, setup, and team workflow
  ```
  </action>

  <verify>
  <automated>grep -c "mixed commit" docs/getting-started.md && grep -c "Concurrent write safety" docs/getting-started.md</automated>
  </verify>

  <acceptance_criteria>
  - `docs/getting-started.md` contains a "Mixed commit strategy" section explaining what gets committed and how to opt in/out
  - `docs/getting-started.md` contains a "Concurrent write safety" section explaining the file locking behavior
  - All existing content (installation, prerequisites, first run, troubleshooting) is preserved
  - `README.md` links to both `docs/configuration.md` and `docs/getting-started.md` (either existing links verified, or new links added)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → reader | Documentation describes configuration and workflow — no runtime security impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Information Disclosure | Docs describe `OPENWOLF_METADATA_DIR` — users may move metadata to insecure location | accept | Documentation includes the absolute-path requirement and security context; user controls deployment choices |
| T-03-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced in this plan |
</threat_model>

<verification>
- `grep -c "OPENWOLF_METADATA_DIR" docs/configuration.md` > 0
- `grep -c "mixed commit" docs/getting-started.md` > 0
- `grep -c "WITH_FILE_LOCK_TTL_MS" docs/configuration.md` > 0
- `grep -c "configuration.md" README.md` > 0 (link exists)
- `grep -c "getting-started.md" README.md` > 0 (link exists)
</verification>

<success_criteria>
- `docs/configuration.md` updated with `OPENWOLF_METADATA_DIR`, `WITH_FILE_LOCK_TTL_MS`, and `.wolf/.gitignore` sections
- `docs/getting-started.md` updated with mixed commit strategy and concurrent write safety sections
- All existing documentation content is preserved
- `README.md` links to both doc files
</success_criteria>

<output>
Create `.planning/phases/03-workflow-improvements/03-05-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

### New documentation content
- `docs/configuration.md` — new section: `.wolf/.gitignore (mixed commit strategy)`
- `docs/configuration.md` — new env vars in table: `OPENWOLF_METADATA_DIR`, `WITH_FILE_LOCK_TTL_MS`
- `docs/getting-started.md` — new section: "Mixed commit strategy" with tracking table and workflow
- `docs/getting-started.md` — new section: "Concurrent write safety" with locking explanation

### Modified files
- `docs/configuration.md` — extended with 3 new documented features
- `docs/getting-started.md` — extended with 2 new sections
- `README.md` — verified/potentially added links to both doc files
</output>

---
phase: 03-p1-modularization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scanner/description-extractor.ts
  - src/scanner/extractors/extract-web.ts
  - src/scanner/extractors/extract-systems.ts
  - src/scanner/extractors/extract-scripting.ts
  - src/scanner/extractors/extract-data.ts
autonomous: true
requirements:
  - SCAN-01
  - SCAN-02

must_haves:
  truths:
    - "description-extractor.ts per-language handlers are extracted into separate modules"
    - "Each extractor module is 5,000 tokens or fewer"
    - "anatomy-scanner.ts continues to import extractDescription from ./description-extractor.js without changes"
  artifacts:
    - path: "src/scanner/extractors/extract-web.ts"
      provides: "TS/JS/Vue/Svelte/Astro/CSS handlers"
      min_lines: 120
    - path: "src/scanner/extractors/extract-systems.ts"
      provides: "Go/Rust/Java/Kotlin/C#/Swift/Dart/Zig handlers"
      min_lines: 180
    - path: "src/scanner/extractors/extract-scripting.ts"
      provides: "PHP/Python/Ruby/Elixir/Lua handlers"
      min_lines: 200
    - path: "src/scanner/extractors/extract-data.ts"
      provides: "SQL/Proto/GraphQL/YAML/TOML handlers"
      min_lines: 60
    - path: "src/scanner/description-extractor.ts"
      provides: "Entry point with extractDescription, extractSmart delegates to new modules"
  key_links:
    - from: "src/scanner/description-extractor.ts"
      to: "src/scanner/extractors/*.ts"
      via: "import and delegate in extractSmart function"
---

<objective>
Extract the monolithic description-extractor.ts into 4 language-family modules under src/scanner/extractors/. The main file retains the extractDescription entry point and delegates to the new modules via extractSmart.

Purpose: SCAN-01 (per-language handlers extracted) and SCAN-02 (each module ≤ 5,000 tokens).
Output: 4 new extractor modules + refactored description-extractor.ts
</objective>

<context>
@src/scanner/description-extractor.ts
@src/scanner/anatomy-scanner.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create extractors directory and module files</name>
  <files>src/scanner/extractors/extract-web.ts, src/scanner/extractors/extract-systems.ts, src/scanner/extractors/extract-scripting.ts, src/scanner/extractors/extract-data.ts</files>
  <read_first>src/scanner/description-extractor.ts (lines 258-287 for extractSmart router, lines 383-868 for web handlers, lines 560-626 for systems handlers, lines 473-558 for scripting handlers, lines 870-940 for data handlers)</read_first>
  <action>
Create src/scanner/extractors/ directory and four language-family modules:

1. **extract-web.ts** — export function extractWeb(content, ext, basename, filePath): string
   - Lines 383-471: extractTsJs (TS/JS/TSX/JSX/MJS/CJS), extractVue, extractSvelte, extractAstro, extractCss
   - Lines 808-868: extractVue, extractSvelte, extractAstro, extractCss (moved from their original positions)

2. **extract-systems.ts** — export function extractSystems(content, ext, basename, filePath): string
   - Lines 560-626: extractGo, extractRust
   - Lines 628-716: extractJava, extractKotlin, extractCSharp
   - Lines 761-806: extractSwift, extractDart
   - Line 284: extractZig
   Each handler function moved verbatim; parameters updated if needed for consistency.

3. **extract-scripting.ts** — export function extractScripting(content, ext, basename, filePath): string
   - Lines 290-381: extractPhp
   - Lines 473-558: extractPython
   - Lines 718-759: extractRuby
   - Lines 942-957: extractElixir
   - Lines 959-965: extractLua
   Each handler function moved verbatim.

4. **extract-data.ts** — export function extractData(content, ext, basename, filePath): string
   - Lines 870-884: extractSql
   - Lines 886-894: extractProto
   - Lines 896-904: extractGraphQL
   - Lines 906-940: extractYaml, extractToml
   Each handler function moved verbatim.

Each module:
- Is a standalone TypeScript file with its own imports (if any internal helpers needed)
- Exports exactly one named function: extractWeb, extractSystems, extractScripting, or extractData
- Returns "" for unhandled extensions (allowing the caller to chain fallbacks)
- Has no dependencies on other extractor modules
  </action>
  <verify>
    <automated>ls src/scanner/extractors/*.ts | wc -l</automated>
    <automated>wc -l src/scanner/extractors/*.ts</automated>
    <automated>tsc --noEmit</automated>
  </verify>
  <done>
    src/scanner/extractors/ contains 4 files: extract-web.ts, extract-systems.ts, extract-scripting.ts, extract-data.ts
    Each file has >50 lines
    tsc --noEmit passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor description-extractor.ts extractSmart to delegate</name>
  <files>src/scanner/description-extractor.ts</files>
  <read_first>src/scanner/description-extractor.ts (lines 258-287 for current extractSmart switch statement)</read_first>
  <action>
Refactor the extractSmart function in description-extractor.ts to import the new modules and delegate:

1. Add imports at top of file (after existing imports):
   ```
   import { extractWeb } from "./extractors/extract-web.js";
   import { extractSystems } from "./extractors/extract-systems.js";
   import { extractScripting } from "./extractors/extract-scripting.js";
   import { extractData } from "./extractors/extract-data.js";
   ```

2. Replace the entire extractSmart function body (lines 259-287) with:
   ```typescript
   function extractSmart(content: string, ext: string, basename: string, filePath: string): string {
     return extractWeb(content, ext, basename, filePath)
       || extractSystems(content, ext, basename, filePath)
       || extractScripting(content, ext, basename, filePath)
       || extractData(content, ext, basename, filePath)
       || "";
   }
   ```

3. Remove all extracted handler functions from description-extractor.ts: extractPhp, extractTsJs, extractPython, extractGo, extractRust, extractJava, extractKotlin, extractCSharp, extractRuby, extractSwift, extractDart, extractVue, extractSvelte, extractAstro, extractCss, extractSql, extractProto, extractGraphQL, extractYaml, extractToml, extractElixir, extractLua, extractZig.

4. Keep in description-extractor.ts: extractDescription, KNOWN_FILES, extractDocblock, extractHeaderComment, extractSmart (refactored), isGenericComment, extractGenericFallback, capDescription.
  </action>
  <verify>
    <automated>tsc --noEmit</automated>
    <automated>grep -c "^function extract" src/scanner/description-extractor.ts</automated>
    <automated>grep "from.*extractors" src/scanner/description-extractor.ts</automated>
  </verify>
  <done>
    extractSmart calls extractWeb || extractSystems || extractScripting || extractData
    No extracted handler functions remain in description-extractor.ts (grep shows 0 matches for ^function extract)
    description-extractor.ts still exports extractDescription at line 122
    tsc --noEmit passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify anatomy-scanner.ts import compatibility</name>
  <files>src/scanner/anatomy-scanner.ts</files>
  <read_first>src/scanner/anatomy-scanner.ts</read_first>
  <action>
Verify that anatomy-scanner.ts continues to work without changes.

1. Read src/scanner/anatomy-scanner.ts to confirm it imports extractDescription from "./description-extractor.js"

2. Run tsc --noEmit to confirm the entire project compiles:
   ```
   tsc --noEmit
   ```

3. Confirm the import path is unchanged: line 4 should be:
   ```
   import { extractDescription } from "./description-extractor.js";
   ```

No changes should be made to anatomy-scanner.ts. The main file description-extractor.ts retains the extractDescription export at the same location.
  </action>
  <verify>
    <automated>grep "from.*description-extractor" src/scanner/anatomy-scanner.ts</automated>
    <automated>tsc --noEmit</automated>
  </verify>
  <done>
    anatomy-scanner.ts line 4: import { extractDescription } from "./description-extractor.js" (unchanged)
    tsc --noEmit passes with no errors
  </done>
</task>

</tasks>

<verification>
- All 4 extractor modules exist under src/scanner/extractors/
- Each extractor module handles its declared language families
- extractSmart in description-extractor.ts delegates to the 4 modules via OR chain
- description-extractor.ts no longer contains the extracted handler functions
- anatomy-scanner.ts compiles without changes
- tsc --noEmit passes for full project
</verification>

<success_criteria>
SCAN-01: description-extractor.ts per-language handlers extracted into src/scanner/extractors/ (4 modules created)
SCAN-02: Each scanner module ≤ 5,000 tokens (verified via wc -l, all 4 modules well under limit per D-01 estimates)
</success_criteria>

<output>
Create .planning/phases/03-p1-modularization/03-01-SUMMARY.md when done
</output>
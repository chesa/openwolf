---
phase: 03-p1-modularization
plan: 03
type: execute
wave: 3
depends_on: []
files_modified:
  - docs/hooks.md
autonomous: true
requirements:
  - HOOK-03

must_haves:
  truths:
    - "docs/hooks.md contains a Worktree Helper section documenting the worktree-helper.js contract"
    - "The section covers exports, types, error handling contract, and usage example"
    - "The section is placed after existing hook descriptions and before Session State"
  artifacts:
    - path: "docs/hooks.md"
      provides: "Worktree Helper section appended after hook lifecycle descriptions"
  key_links:
    - from: "docs/hooks.md Worktree Helper section"
      to: "src/hooks/worktree-helper.ts"
      via: "import path and type references"
---

<objective>
Document the worktree-helper.js hook contract in docs/hooks.md with an API reference, type documentation, error handling contract, and usage example.

Purpose: HOOK-03 (docs/hooks.md documents worktree-helper.js contract).
Output: New "Worktree Helper" section appended to docs/hooks.md
</objective>

<context>
@docs/hooks.md
@src/hooks/worktree-helper.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Worktree Helper section to docs/hooks.md</name>
  <files>docs/hooks.md</files>
  <read_first>docs/hooks.md (full file to understand placement before Session State section)</read_first>
  <action>
Add a new "Worktree Helper (`worktree-helper.js`)" section to docs/hooks.md before the "## Session State" section (which starts at line 138).

The section should include:

1. **Purpose** — Git worktree detection for session isolation per branch

2. **Exports table** with:
   - `detectWorktreeContextRaw(dir: string): WorktreeContext` — Detects whether dir is in a git worktree; returns context object
   - `isNotARepoError(err: unknown): boolean` — Classifies error as "not a git repo" (exit status 128)
   - `isMissingGitError(err: unknown): boolean` — Classifies error as "git binary not found" (ENOENT)
   - `isTimeoutError(err: unknown): boolean` — Classifies error as "git command timed out" (SIGTERM/ETIMEDOUT)

3. **Types** — WorktreeId and WorktreeContext type shapes:
   ```
   type WorktreeId = string & { readonly __brand: "WorktreeId" };
   
   type WorktreeContext =
     | { isWorktree: false; mainRepoRoot: string; worktreePath: string; branch: string }
     | { isWorktree: true; mainRepoRoot: string; worktreePath: string; worktreeId: WorktreeId; branch: string };
   ```

4. **Error Handling Contract** — table:
   | Error | Condition | Callers Should |
   |-------|-----------|----------------|
   | Not a repo | git rev-parse exits 128 | Use main repo root directly |
   | Git missing | git binary not found (ENOENT) | Log warning, fall back to single-repo mode |
   | Timeout | Command exceeds 2s (SIGTERM) | Log warning, treat as single-repo mode |

5. **Usage Example** — minimal code snippet showing detectWorktreeContextRaw call and error classification using the error classifiers.

Format the section using markdown tables and code blocks consistent with the existing hook documentation style in hooks.md.
  </action>
  <verify>
    <automated>grep -c "Worktree Helper" docs/hooks.md</automated>
    <automated>grep -c "detectWorktreeContextRaw" docs/hooks.md</automated>
    <automated>grep -c "isNotARepoError\|isMissingGitError\|isTimeoutError" docs/hooks.md</automated>
  </verify>
  <done>
    docs/hooks.md contains "Worktree Helper" section heading
    Section documents detectWorktreeContextRaw, isNotARepoError, isMissingGitError, isTimeoutError
    Section includes WorktreeContext type
    Section includes usage example
    Section placed before "## Session State" heading
  </done>
</task>

</tasks>

<verification>
- docs/hooks.md has new Worktree Helper section
- Section documents all 4 exports from worktree-helper.ts
- Section includes WorktreeContext type definition
- Section documents error handling contract for 3 error cases
- Section includes usage example
</verification>

<success_criteria>
HOOK-03: docs/hooks.md documents worktree-helper.js hook contract (exports, types, error handling, usage example)
</success_criteria>

<output>
Create .planning/phases/03-p1-modularization/03-03-SUMMARY.md when done
</output>
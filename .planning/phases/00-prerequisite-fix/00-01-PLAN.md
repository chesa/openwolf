---
phase: 00-prerequisite-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/hook-settings.ts
  - tests/cli/init.test.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "HOOK_FILES is no longer exported from hook-settings.ts"
    - "No production code in src/ references HOOK_FILES"
    - "Tests verify getHookFileNames() dynamic discovery behavior instead of importing a static constant"
    - "All existing tests continue to pass"
  artifacts:
    - path: "src/cli/hook-settings.ts"
      provides: "Hook registration — HOOK_FILES constant removed"
      does_not_contain: "HOOK_FILES"
    - path: "tests/cli/init.test.ts"
      provides: "Dynamic discovery tests for getHookFileNames()"
      contains: "getHookFileNames"
  key_links:
    - from: "tests/cli/init.test.ts"
      to: "src/cli/hook-copy.ts"
      via: "import getHookFileNames"
      pattern: "getHookFileNames"
    - from: "src/cli/hook-settings.ts"
      to: "src/cli/hook-copy.ts"
      via: "(no link — HOOK_FILES removed, hook-copy.ts handles discovery)"
      pattern: "NO_MORE_HOOK_FILES"
---

<objective>
Remove the vestigial `HOOK_FILES` constant from `src/cli/hook-settings.ts` and migrate tests to validate dynamic discovery via `getHookFileNames()`.

Purpose: The HOOK_FILES constant is dead code — no production code imports it. It was retained only because `tests/cli/init.test.ts` imported it. Dynamic discovery via `hook-copy.ts` (`getHookFileNames()` using `fs.readdirSync`) replaced the static file list in Phase 3. This cleanup completes that transition by removing the dead constant and rewriting tests to validate the discovery mechanism.

Output: Cleaner source file (HOOK_FILES removed from hook-settings.ts), updated tests that verify dynamic discovery behavior, all tests passing.
</objective>

<execution_context>
@/Users/bfs/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/bfs/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-prerequisite-fix/00-CONTEXT.md
@.planning/phases/00-prerequisite-fix/00-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md

# Source files to modify
@src/cli/hook-settings.ts
@tests/cli/init.test.ts
@src/cli/hook-copy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove HOOK_FILES from hook-settings.ts</name>
  <files>src/cli/hook-settings.ts</files>
  <read_first>src/cli/hook-settings.ts (lines 97–114 — the HOOK_FILES block)</read_first>
  <action>
Remove the `export const HOOK_FILES = [...]` block (lines 98–114) and its preceding blank line at line 97. This is a standalone `export const` — nothing else in the file references it.

After removal, add a brief inline comment documenting the historical purpose:

```typescript
// HOOK_FILES (static file list) removed in v0 — dynamic discovery via
// hook-copy.ts getHookFileNames() replaced it.
```

Place this comment at the location where HOOK_FILES was (between the closing `};` of HOOK_SETTINGS on line 96 and the JSDoc for `isOpenWolfHook` that starts on line 116).

Do NOT touch any other part of the file. Specifically:
- Do NOT modify `HOOK_SETTINGS` (lines 33–96)
- Do NOT modify `WOLF_ROOT_SHELL` (lines 19–20)
- Do NOT modify `isOpenWolfHook` (lines 123–131)
- Do NOT modify `replaceOpenWolfHooks` (lines 137–169)
- Do NOT modify `hookCmd` helper (lines 22–23)
  </action>
  <acceptance_criteria>
    - `grep -c "export const HOOK_FILES" src/cli/hook-settings.ts` returns 0
    - `grep -c "HOOK_FILES" src/cli/*.ts` returns 0
    - `HOOK_SETTINGS`, `isOpenWolfHook`, `replaceOpenWolfHooks`, `WOLF_ROOT_SHELL` exports are unchanged (no source assertions broken)
    - A comment mentioning "HOOK_FILES" and "dynamic discovery" exists where the constant was removed
  </acceptance_criteria>
  <verify>
    <automated>grep -c "export const HOOK_FILES" src/cli/hook-settings.ts; test $? -eq 1</automated>
  </verify>
  <done>HOOK_FILES removed from hook-settings.ts, no production code references it, historical comment added, all other exports intact</done>
</task>

<task type="auto">
  <name>Task 2: Update init.test.ts to test dynamic discovery instead of HOOK_FILES</name>
  <files>tests/cli/init.test.ts</files>
  <read_first>
    tests/cli/init.test.ts (lines 254–273 — the "hook-file copy list" describe block to replace)
    src/cli/hook-copy.ts (lines 32–40 — getHookFileNames signature)
  </read_first>
  <action>
Replace the "hook-file copy list" describe block (currently lines 257–273) with a new describe block that tests `getHookFileNames()` from `hook-copy.ts` using the temp-directory fixture pattern (established in `hook-settings.test.ts:25–74`).

Use RESEARCH.md Option B (simpler approach — tests the discovery mechanism without freezing an exact file list):

1. Add imports at the top of the test file (not inside the describe block — add after the existing `vi.mock` imports around line 22):
   - `import { mkdtempSync, writeFileSync, rmSync } from "node:fs";`
   - `import { tmpdir } from "node:os";`
   - `import * as path from "node:path";`

2. Replace the entire "hook-file copy list" describe block (lines 257–273) with:

```typescript
describe("hook-file copy list", () => {
  it("discovers all .js files and filters out non-js files", () => {
    const dir = realpathSync(mkdtempSync(path.join(tmpdir(), "openwolf-hook-copy-")));
    try {
      writeFileSync(path.join(dir, "shared.js"), "");
      writeFileSync(path.join(dir, "worktree-helper.js"), "");
      writeFileSync(path.join(dir, "not-a-hook.txt"), ""); // excluded by .endsWith(".js")

      const { getHookFileNames } = await import("../../src/cli/hook-copy.js");
      const files = getHookFileNames(dir);

      expect(files).toContain("shared.js");
      expect(files).toContain("worktree-helper.js");
      expect(files).not.toContain("not-a-hook.txt");
      expect(files.length).toBe(2);
    } finally {
      rmSync(dir, { recursive: true, force: true });
    }
  });
});
```

Note the use of `realpathSync` wrapping `mkdtempSync` — this matches the established pattern from `hook-settings.test.ts:25` (macOS `/var/` vs `/private/var/` symlink resolution). Import `realpathSync` from `node:fs` alongside `mkdtempSync`.

3. Remove the `HOOK_SETTINGS` import reference for the old HOOK_FILES block — the test file currently imports `{ HOOK_SETTINGS, isOpenWolfHook, replaceOpenWolfHooks }` on line 6. That import line does NOT include HOOK_FILES today (only the describe block does via dynamic `await import()`), so no import-line changes are needed. Double-check after editing.

Verify the test file no longer contains the string "HOOK_FILES" anywhere.
  </action>
  <acceptance_criteria>
    - `grep -c "HOOK_FILES" tests/cli/init.test.ts` returns 0
    - The "hook-file copy list" describe block tests `getHookFileNames()` behavior (not a static list)
    - Test validates: `.js` files are included, non-`.js` files are excluded, correct count
    - Test uses `mkdtempSync`/`rmSync` temp-directory pattern (not relying on `dist/hooks/` existing)
    - `npx vitest run tests/cli/init.test.ts` passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/cli/init.test.ts</automated>
  </verify>
  <done>"hook-file copy list" tests verify getHookFileNames() dynamic discovery, no HOOK_FILES references remain in test file, all tests pass</done>
</task>

</tasks>

## Artifacts This Phase Produces

**Symbols removed:**
- `HOOK_FILES` — removed from `src/cli/hook-settings.ts` (previously exported as `export const HOOK_FILES: string[]`)

**Symbols tested (existing, from `src/cli/hook-copy.ts`):**
- `getHookFileNames(sourceDir: string): string[]` — dynamic discovery function (now tested in init.test.ts)

**Files modified:**
- `src/cli/hook-settings.ts` — HOOK_FILES constant removed, historical comment added
- `tests/cli/init.test.ts` — "hook-file copy list" describe block rewritten to test `getHookFileNames()` instead of importing HOOK_FILES

**No new symbols created by this phase.**

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase removes a dead constant and updates a test. No trust boundary introduced or modified. No user-facing code changed. No external data processed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Tampering | npm/pip/cargo installs | accept | No packages installed by this phase — pure source-code cleanup. |
</threat_model>

<verification>
Static analysis:
- `grep -c "HOOK_FILES" src/cli/*.ts` returns 0 (no production references)
- `grep -c "HOOK_FILES" tests/cli/init.test.ts` returns 0 (no test references)

Test suite:
- `npx vitest run tests/cli/init.test.ts` passes
- `npx vitest run` (full suite) passes
</verification>

<success_criteria>
- HOOK_FILES constant fully removed from hook-settings.ts
- Historical comment documents the removal
- init.test.ts tests `getHookFileNames()` behavior via temp-directory fixtures
- No "HOOK_FILES" string remains in any source or test file
- Full Vitest suite green
</success_criteria>

<output>
Create `.planning/phases/00-prerequisite-fix/00-01-SUMMARY.md` when done
</output>

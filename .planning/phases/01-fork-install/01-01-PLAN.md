---
phase: 01-fork-install
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/install-dev.sh, package.json, README.md]
autonomous: true
requirements: [R-01-01, R-01-02, R-01-03]

must_haves:
  truths:
    - "Development script exists and is executable"
    - "New install:dev script added to package.json"
    - "README.md updated with installation instructions"
  artifacts:
    - path: "scripts/install-dev.sh"
      provides: "Automated local environment setup"
    - path: "package.json"
      provides: "Defined install:dev script"
    - path: "README.md"
      provides: "Installation documentation"
  key_links:
    - from: "README.md"
      to: "scripts/install-dev.sh"
      via: "documented command"
---

<objective>
This plan sets up automated development environment installation for OpenWolf to streamline team onboarding.

Purpose: Reduce onboarding friction by providing a single script for dependency setup and global linking.
Output: `scripts/install-dev.sh` (new), updated `package.json`, and updated `README.md`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fork-install/01-RESEARCH.md
@package.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/install-dev.sh</name>
  <files>scripts/install-dev.sh</files>
  <action>Create a new bash script `scripts/install-dev.sh` that automates local environment setup. It must:
1. Start with `#!/bin/bash` and `set -e`.
2. Run `pnpm install`, `pnpm build`, and `pnpm link --global`.
3. Be made executable (`chmod +x`).
Ensure no hardcoded absolute paths are used.</action>
  <verify>
    <automated>test -x scripts/install-dev.sh && ./scripts/install-dev.sh --help</automated>
  </verify>
  <done>scripts/install-dev.sh exists, is executable, and runs the required setup commands.</done>
</task>

<task type="auto">
  <name>Task 2: Update package.json and README.md</name>
  <files>package.json, README.md</files>
  <action>Perform two updates:
1. In `package.json`, add an `install:dev` script under `"scripts"`: `"install:dev": "pnpm install && pnpm build && pnpm link --global"`.
2. In `README.md`, update the "Installation" section to include the new development setup instructions: `./scripts/install-dev.sh`.</action>
  <verify>
    <automated>grep -q "install:dev" package.json && grep -q "install-dev.sh" README.md</automated>
  </verify>
  <done>package.json contains install:dev script, README.md contains instructions for it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripts/install-dev.sh -> system | Executes commands that modify global node environment (via pnpm link). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | scripts/install-dev.sh | mitigate | Ensure script is only executable by project owner, and uses `pnpm` (standard tool, safer than manual manipulation). |
| T-01-02 | Tampering | npm/pnpm installs | mitigate | Rely on `pnpm`'s integrity checking of lockfile. |
</threat_model>

<verification>
Check that `install:dev` can be executed via `pnpm install:dev` and that `scripts/install-dev.sh` is executable.
</verification>

<success_criteria>
1. `scripts/install-dev.sh` is created and executable.
2. `package.json` has `install:dev` script.
3. `README.md` instructions are updated.
</success_criteria>

<output>
Create `.planning/phases/01-fork-install/01-01-SUMMARY.md` when done
</output>

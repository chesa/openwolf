---
phase: 01-fork-install
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified: [docs/DEVELOPMENT.md]
autonomous: true
requirements: [R-01-04]

must_haves:
  truths:
    - "docs/DEVELOPMENT.md contains troubleshooting information for pnpm link --global failure (PATH issue)"
  artifacts:
    - path: "docs/DEVELOPMENT.md"
      provides: "Updated troubleshooting section"
      contains: "troubleshooting"

---

<objective>
Document the `pnpm link --global` environment path dependency to improve developer onboarding and reduce setup friction.

Output: Updated `docs/DEVELOPMENT.md` with a troubleshooting subsection in the "Local Setup" area.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Update docs/DEVELOPMENT.md with pnpm link troubleshooting</name>
  <files>docs/DEVELOPMENT.md</files>
  <action>
    Add a troubleshooting note to `docs/DEVELOPMENT.md` explaining that if `pnpm link --global` fails with a path error, the user needs to add the pnpm bin directory to their PATH. Suggest running `pnpm setup`.
  </action>
  <acceptance_criteria>
    - `grep -c "troubleshooting" docs/DEVELOPMENT.md` is >= 1.
    - `grep -c "pnpm link" docs/DEVELOPMENT.md` is >= 1.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "troubleshooting" docs/DEVELOPMENT.md && grep -q "pnpm link" docs/DEVELOPMENT.md</automated>
  </verify>
  <done>docs/DEVELOPMENT.md updated with troubleshooting section.</done>
</task>
</tasks>
